import{_ as t,o as s,c as a,a2 as o}from"./chunks/framework.uw_ZoRon.js";const m=JSON.parse('{"title":"Architecture — Rust + Tokio + Wasmtime, mapped to the BEAM","description":"","frontmatter":{},"headers":[],"relativePath":"about/architecture.md","filePath":"about/architecture.md"}'),r={name:"about/architecture.md"};function n(i,e,c,d,h,l){return s(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="architecture-—-rust-tokio-wasmtime-mapped-to-the-beam" tabindex="-1">Architecture — Rust + Tokio + Wasmtime, mapped to the BEAM <a class="header-anchor" href="#architecture-—-rust-tokio-wasmtime-mapped-to-the-beam" aria-label="Permalink to &quot;Architecture — Rust + Tokio + Wasmtime, mapped to the BEAM&quot;">​</a></h1><p>RUSM rebuilds Erlang&#39;s actor model — lightweight processes, message passing, supervision — on a modern Rust foundation. The bet is in the choice of layers: <strong>Rust</strong> gives native speed with no garbage collector (so no stop-the-world pauses on tail latency), <strong>Tokio</strong> gives an M:N scheduler that already does what the BEAM&#39;s schedulers do, and <strong>Wasmtime</strong> gives each actor <em>real</em> memory isolation rather than the BEAM&#39;s softer task-level boundary. Three layers, each with one job; together they reproduce — and in places beat — the BEAM.</p><p>This page maps that correspondence layer by layer, then states the one architectural invariant the whole design rests on. For where each capability lands in the build, see <a href="/rusm/about/roadmap">the roadmap</a>; for an honest assessment of the trade-offs, see the <a href="/rusm/about/design-analysis">design analysis</a>.</p><h2 id="rust-→-the-fast-safe-host" tabindex="-1">Rust → the fast, safe host <a class="header-anchor" href="#rust-→-the-fast-safe-host" aria-label="Permalink to &quot;Rust → the fast, safe host&quot;">​</a></h2><p>The host is Rust because the host work is unforgiving: the scheduler, cross-memory message copying, networking, and host functions all sit on the hot path. Native speed with <strong>no garbage collector</strong> means no stop-the-world pauses hurting tail latency, and a tiny per-process footprint — what lets the spawn storm sustain <strong>~2.4M spawns/sec</strong>.</p><p>Note the boundary, though: Rust&#39;s speed is the <em>host</em>. Guest actor code is <strong>Wasm</strong>, compiled to native by Wasmtime&#39;s Cranelift JIT — so guest speed is Wasmtime&#39;s, not the host&#39;s.</p><h2 id="tokio-→-the-process-scheduler-async-i-o" tabindex="-1">Tokio → the process scheduler + async I/O <a class="header-anchor" href="#tokio-→-the-process-scheduler-async-i-o" aria-label="Permalink to &quot;Tokio → the process scheduler + async I/O&quot;">​</a></h2><p>A multi-threaded <strong>work-stealing</strong> runtime that multiplexes millions of lightweight tasks over a few OS threads (M:N) — exactly what BEAM schedulers do. The mapping is direct: <strong>one RUSM process (a Wasm instance) = one Tokio task.</strong> Tokio also gives us async networking (TCP, and QUIC for the cluster) and timers, so the whole I/O surface is one runtime rather than a patchwork.</p><h2 id="wasmtime-→-fast-isolated-sandboxed-guests" tabindex="-1">Wasmtime → fast, isolated, sandboxed guests <a class="header-anchor" href="#wasmtime-→-fast-isolated-sandboxed-guests" aria-label="Permalink to &quot;Wasmtime → fast, isolated, sandboxed guests&quot;">​</a></h2><p>Where the BEAM isolates actors at the task level, RUSM isolates them at the <em>memory</em> level. Wasmtime compiles and sandboxes each actor — isolation gives both fault tolerance and per-actor permissions. Its <strong>fiber-based async support</strong> is what lets a guest write straight-line blocking code: a &quot;blocking&quot; call is suspended so the Tokio task can <code>await</code> underneath it — the blocking→async trick. See <a href="/rusm/deep-dive/fibers-and-blocking-to-async">fibers &amp; blocking→async</a>.</p><h2 id="beyond-plain-tokio-fair-preemption" tabindex="-1">Beyond plain Tokio: fair preemption <a class="header-anchor" href="#beyond-plain-tokio-fair-preemption" aria-label="Permalink to &quot;Beyond plain Tokio: fair preemption&quot;">​</a></h2><p>One gap remains between Tokio and the BEAM, and RUSM closes it. Tokio is <em>cooperative</em> — a tight <code>loop {}</code> would hog a worker and starve its neighbours. The BEAM avoids this with reduction counting; RUSM uses <strong>Wasmtime epoch interruption</strong> to force even an infinite-loop guest to yield, so a misbehaving actor can&#39;t take the scheduler hostage. See <a href="/rusm/deep-dive/epoch-preemption">epoch preemption</a>.</p><h2 id="mapping-table" tabindex="-1">Mapping table <a class="header-anchor" href="#mapping-table" aria-label="Permalink to &quot;Mapping table&quot;">​</a></h2><p>Put together, every BEAM concept has a concrete RUSM counterpart:</p><table tabindex="0"><thead><tr><th>BEAM</th><th>RUSM</th></tr></thead><tbody><tr><td>process</td><td>Wasm instance + Tokio task</td></tr><tr><td>scheduler</td><td>Tokio work-stealing runtime</td></tr><tr><td>reduction counting</td><td>Wasmtime epoch interruption</td></tr><tr><td>mailbox / <code>send</code></td><td>host-copied message + async channel</td></tr><tr><td>link / monitor / supervisor</td><td>trap propagation + link table</td></tr><tr><td><code>:global</code> registry</td><td>gossiped distributed registry (<code>rusm-cluster</code>)</td></tr><tr><td><code>Node.connect</code> / epmd</td><td>QUIC + mutual-TLS node transport (<code>rusm-cluster</code>)</td></tr><tr><td><code>iex --remsh</code> / observer</td><td><code>rusm attach</code> + dashboard observer</td></tr></tbody></table><h2 id="architectural-invariant-—-a-wasm-free-core" tabindex="-1">Architectural invariant — a Wasm-free core <a class="header-anchor" href="#architectural-invariant-—-a-wasm-free-core" aria-label="Permalink to &quot;Architectural invariant — a Wasm-free core&quot;">​</a></h2><p>The mapping table makes the layers look co-equal, but they are not: one of them is the heart, and the rest are swappable around it. RUSM&#39;s heart is the <strong>Erlang/OTP actor model in pure Rust</strong>, and it must stay that way — <strong>Wasm must not bleed into code where it is irrelevant.</strong> This is RUSM&#39;s single most consequential design decision, because it is enforced by the compiler rather than by convention:</p><ul><li><strong><code>crates/rusm-otp</code></strong> — the core: processes, mailboxes, <code>Signal</code>s, links, monitors, supervisors, registry, scheduler, and native connectivity. Generic over an abstract process <strong>body</strong>. <strong>It must not depend on <code>wasmtime</code> or name any Wasm type.</strong> It is usable on its own as a native-Rust OTP/actor library (an &quot;rustOTP&quot;). It is <strong>built incrementally across Phases 1–5</strong> (process core → messaging → supervision → management → connectivity) — the OTP layer is the whole of those phases, not just Phase 1. (Networking may live in a sibling Wasm-free crate, e.g. <code>rusm-net</code>, but is part of this layer.)</li><li><strong><code>crates/rusm-wasm</code></strong> — the <em>optional</em> execution backend (Phase 6): implements the body trait with Wasmtime instances. The <strong>only</strong> crate that touches <code>wasmtime</code>.</li><li><strong><code>rusm</code></strong> — the runtime = <code>rusm-otp</code> + <code>rusm-wasm</code> + host APIs + CLI.</li></ul><p>The dependency graph <strong>enforces</strong> the boundary: because <code>rusm-otp</code> has no <code>wasmtime</code> dependency, the compiler <em>guarantees</em> the actor model stands alone and Wasmtime is a swappable backend — a structural fact, not a claim. Even messages stay Wasm-agnostic: bytes plus opaque resource handles (<code>Arc&lt;dyn Any + Send + Sync&gt;</code>), no Wasm types.</p><h2 id="how-the-crates-stack-up" tabindex="-1">How the crates stack up <a class="header-anchor" href="#how-the-crates-stack-up" aria-label="Permalink to &quot;How the crates stack up&quot;">​</a></h2><p>That invariant shows up directly in the dependency layout — <code>rusm-wasm</code> depends on <code>rusm-otp</code>, never the reverse, and everything else plugs in around the core:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes rose-pine-dawn rose-pine-moon catppuccin-latte catppuccin-mocha vitesse-light vitesse-dark one-light one-dark-pro vp-code" tabindex="0"><code><span class="line"><span>                       ┌─ bridges/wasip1 (core modules + raw rusm::* ABI + byte streams)</span></span>
<span class="line"><span>rusm-otp  ◀── rusm-wasm ┼─ bridges/wasip2 (components + rusm:runtime WIT actor world)</span></span>
<span class="line"><span>(Wasm-free │            └─ bridges/wasip3 (WASI @0.3.0 async interfaces)</span></span>
<span class="line"><span> OTP core) │            caps.rs (default-deny profiles) · epoch · pooling + CoW</span></span>
<span class="line"><span>           │</span></span>
<span class="line"><span>           └── rusm-cli (app model: rusm.toml [components.&lt;name&gt;], build/run/dev, attach)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>observability ── rusm-metrics + rusm-observer ─→ rusm-bench (runner + WebSocket</span></span>
<span class="line"><span>                 server) ─→ dashboard / rusm attach</span></span></code></pre></div><p><code>rusm-otp</code> is the Wasm-free core; <code>rusm-wasm</code> is the <em>only</em> crate that touches Wasmtime and drives the core through its public API. The observability stack (metrics/observer/bench/dashboard) plugs into any node. Distribution (the <code>rusm-cluster</code> QUIC + TLS transport, Phase 9) is also Wasm-free, over <code>rusm-otp</code>, as is durable storage (the <code>rusm-kv</code> redb-backed key-value store, surfaced to guests by <code>rusm-wasm</code>); remaining layers are on <a href="/rusm/about/roadmap">the roadmap</a>.</p>`,23)])])}const u=t(r,[["render",n]]);export{m as __pageData,u as default};
