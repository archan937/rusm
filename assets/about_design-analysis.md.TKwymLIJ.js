import{_ as t,o as s,c as o,a2 as a}from"./chunks/framework.uw_ZoRon.js";const u=JSON.parse('{"title":"Design analysis","description":"","frontmatter":{},"headers":[],"relativePath":"about/design-analysis.md","filePath":"about/design-analysis.md"}'),n={name:"about/design-analysis.md"};function r(i,e,d,c,l,h){return s(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="design-analysis" tabindex="-1">Design analysis <a class="header-anchor" href="#design-analysis" aria-label="Permalink to &quot;Design analysis&quot;">​</a></h1><p>A runtime is only as trustworthy as it is honest about itself. This page is RUSM&#39;s self-assessment, kept current as the project evolves: what the design genuinely gets right, how it performs, where the real opportunities are, and the known lackings — each with its status, so nothing is quietly swept aside. The &quot;lackings&quot; table below is the part to read first if you&#39;re sceptical; most of what it once tracked has since shipped, and the rows that haven&#39;t are stated plainly.</p><h2 id="where-it-s-superior" tabindex="-1">Where it&#39;s superior <a class="header-anchor" href="#where-it-s-superior" aria-label="Permalink to &quot;Where it&#39;s superior&quot;">​</a></h2><ul><li><strong>A Wasm-free OTP core.</strong> <code>rusm-otp</code> (processes, mailboxes, links/monitors/ supervision, registry, timers, TCP) has <em>zero</em> Wasmtime dependency — enforced by the dependency graph. The actor model is a standalone Rust library; Wasmtime is a swappable backend. Reusable, testable in isolation, uncontaminated.</li><li><strong>Cheaper processes than Lunatic.</strong> One channel per process (exit signals ride the mailbox; kill is an abort-handle flag-flip) vs Lunatic&#39;s two channels.</li><li><strong>A component-model host — an axis Lunatic lacks.</strong> Core modules <strong>+</strong> components (WASI p2/p3) <strong>+</strong> TS/JS, all instance-per-process actors, with a WIT actor world callable from any language. Composition is <strong>message passing, not a lattice</strong>.</li><li><strong>No execution-time cap</strong> (vs wasmCloud&#39;s 30s); long-lived supervised actors. Epoch preemption on a <strong>dedicated OS thread</strong> (preemption that can&#39;t be starved).</li><li><strong>Default-deny capabilities per process</strong> — now including the actor process- control surface (see #2 below).</li></ul><h2 id="performance" tabindex="-1">Performance <a class="header-anchor" href="#performance" aria-label="Permalink to &quot;Performance&quot;">​</a></h2><ul><li>~2.4M native spawns/s, ~440–475k Wasm spawns/s, ~21M msgs/s (p50 &lt;1µs), fairness 50M→400M+ ops/s, <strong>15+ GB/s</strong> cross-process streaming. Pooling + CoW + <code>InstancePre</code> + precomputed-export-index make instance-per-process cheap; Tokio&#39;s work-stealing scheduler + mpsc do the heavy lifting (battle-proven, not reinvented).</li><li>The native→Wasm ~5× gap is the memory-isolation tax, paid once. Streaming&#39;s two copies are irreducible across isolation boundaries.</li><li>Most numbers are in-process/loopback — they prove the runtime isn&#39;t the bottleneck, not network throughput.</li></ul><h2 id="opportunities" tabindex="-1">Opportunities <a class="header-anchor" href="#opportunities" aria-label="Permalink to &quot;Opportunities&quot;">​</a></h2><p>Most of what this section once tracked has since shipped:</p><ul><li><strong>HTTP(S)/WS(S)/SSE serving via <code>wasi:http</code></strong> — Phase 11 (it also unlocked <code>fetch</code> in TS guests: a Tokio HTTP client + fiber suspension).</li><li><strong>Distributed cluster</strong> — Phase 9 (<code>rusm-cluster</code>, QUIC+TLS): single-node → horizontal.</li><li><strong>On-demand instance tier above the pool</strong> — Phase 10.</li><li><strong>Guest crates</strong> (<code>rusm-rs</code>/<code>rusm-ts</code>/<code>rusm-go</code>, service macros, typed clients, in-guest <code>Supervisor</code> strategies) — Phase 8.</li></ul><p>The one genuinely open opportunity is <strong>a true head-to-head benchmark vs Lunatic</strong>: the architectural case is made (pooling + CoW + epoch vs on-demand + fuel); what&#39;s missing is numbers from both runtimes on the same box.</p><h2 id="lackings-—-status" tabindex="-1">Lackings — status <a class="header-anchor" href="#lackings-—-status" aria-label="Permalink to &quot;Lackings — status&quot;">​</a></h2><table tabindex="0"><thead><tr><th>#</th><th>Lacking</th><th>Status</th></tr></thead><tbody><tr><td>1</td><td>Wasm-instance concurrency ceiling</td><td><strong>Solved (Phase 10)</strong> — configurable via <code>WasmRuntime::with_limits</code> (default raised 256→1024, lazy virtual reservation), plus an <strong>on-demand overflow tier</strong> (<code>WasmRuntime::with_overflow</code>) above the pool: past the pooled cap, spawns come from an on-demand engine, so the live Wasm-process count is bounded by <strong>available memory</strong>, not a compile-time size.</td></tr><tr><td>2</td><td>Actor ABI not capability-scoped (untrusted code could kill/enumerate any process)</td><td><strong>Solved</strong> — default-deny <code>allow_process_control</code>; a sandboxed guest manages only itself. Enforced on both bridges, gate-tested.</td></tr><tr><td>3</td><td>Unbounded mailboxes (a fast producer can grow a slow consumer&#39;s mailbox)</td><td><strong>Solved (Phase 10, opt-in)</strong> — <code>Runtime::with_mailbox_capacity</code> sheds <em>user</em> messages past capacity (system/exit signals are never dropped), reusing the opt-in depth counter. Unbounded stays the Erlang-compatible default; bounding the <em>serve</em> path by default is the remaining Phase 12 item.</td></tr><tr><td>4</td><td>Shallow supervision (links/monitors/restart-bool, not OTP strategies)</td><td><strong>Solved (in-guest)</strong> — Phase 8 ships an in-guest <code>Supervisor</code> (<code>one_for_one</code>/<code>one_for_all</code>/<code>rest_for_one</code> + <code>max_restarts</code>) over a <code>monitor</code> ABI, in both <code>rusm-rs</code> and <code>rusm-ts</code>.</td></tr><tr><td>5</td><td>DX/toolchain friction</td><td><strong>Largely a non-issue</strong> — a TS dev needs only Bun (the <code>rusm-ts</code> npm package + <code>rusm dev</code> watch/reload); wasi-sdk is a one-time <em>maintainer</em> build dep (the runner is prebuilt). <code>rusm new &lt;name&gt;</code> scaffolds a ready-to-serve app in one command.</td></tr><tr><td>6</td><td>TS guests lacked Web APIs</td><td><strong>Solved</strong> — full Web API polyfills (<code>bridge/webapi.js</code>: TextEncoder/URL/Headers/ReadableStream/…), transparent to the dev. <code>fetch</code> awaits <code>wasi:http</code> (the one genuinely network-bound API).</td></tr><tr><td>7</td><td>Selective receive is O(n) over the save queue</td><td><strong>Accepted</strong> — inherent to selective-receive semantics (so is the BEAM&#39;s); the common <code>recv</code> path is O(1).</td></tr><tr><td>8</td><td>Distribution is roadmap; <code>distributed-fanout</code> is synthetic</td><td><strong>Solved (Phase 9)</strong> — the Wasm-free <code>rusm-cluster</code> QUIC+TLS transport: cross-node send, gossiped global registry, remote spawn, live attach (~550k cross-node msgs/s). The <code>distributed-fanout</code> dashboard scenario now runs on this <strong>real</strong> engine — no synthetic scenarios remain.</td></tr></tbody></table><h2 id="verdict" tabindex="-1">Verdict <a class="header-anchor" href="#verdict" aria-label="Permalink to &quot;Verdict&quot;">​</a></h2><p>The core architecture — the Wasm-free OTP boundary, the component-model host with message-passing composition, the no-time-cap lifetime model, and a <strong>capability-scoped</strong> actor ABI — is differentiated and sound. The two items once flagged highest-priority (the <strong>on-demand instance tier</strong> #1 and <strong>bounded mailboxes</strong> #3) have since shipped in Phase 10. What remains is operational edge-hardening (<strong>Phase 12</strong>): HTTP request-body/timeout admission control, default-bounded <em>serve-path</em> mailboxes, and authenticated cluster gossip — network-edge and peer-trust gaps, not architectural ones.</p>',14)])])}const g=t(n,[["render",r]]);export{u as __pageData,g as default};
