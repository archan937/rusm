import{_ as o,o as t,c as s,a2 as r}from"./chunks/framework.uw_ZoRon.js";const m=JSON.parse('{"title":"Roadmap — TDD baby steps","description":"","frontmatter":{},"headers":[],"relativePath":"about/roadmap.md","filePath":"about/roadmap.md"}'),n={name:"about/roadmap.md"};function a(d,e,c,i,l,p){return t(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="roadmap-—-tdd-baby-steps" tabindex="-1">Roadmap — TDD baby steps <a class="header-anchor" href="#roadmap-—-tdd-baby-steps" aria-label="Permalink to &quot;Roadmap — TDD baby steps&quot;">​</a></h1><p>This page is the build log: how RUSM was assembled in twelve disciplined phases, what each one shipped, and what still lies ahead. The interesting decision is the <em>ordering</em>. RUSM is built foundation-first — the Erlang/OTP actor model in pure Rust comes first (Phases 1–5, on native process bodies), so the actor core is real, tested, and measurable on the dashboard <em>before any WebAssembly exists</em>. Wasmtime slots in only at Phase 6, as a swappable execution backend rather than the foundation; distribution, scale, and serving build on top from there. The payoff of that order is concrete: the hardest part — a correct, fast actor runtime — is proven on its own before isolation is layered on, and the compiler enforces that the core never grows a Wasm dependency.</p><p>Each phase writes the failing test first, then implements until green, and leaves <code>cargo test</code> passing. Every phase &quot;graduates&quot; a dashboard scenario from synthetic data to real measurements.</p><blockquote><p><strong>Foundation-first ordering.</strong> The Erlang model — processes, messaging, supervision/fault-tolerance, management, connectivity — is the foundation and comes first, built on <strong>native Rust process bodies</strong> so it&#39;s real and measurable early. <strong>Wasmtime is the execution <em>backend</em>, slotted in at Phase 6</strong>: the actor layer is designed wasm-ready, so swapping a process body from a native closure to a sandboxed Wasm instance is additive, not a rewrite. That&#39;s also when &quot;task-level&quot; fault isolation becomes &quot;true memory isolation&quot;.</p><p><strong>Crate mapping:</strong> Phases 1–5 build the Wasm-free OTP core (<code>rusm-otp</code> — usable standalone); Phase 6 adds the <code>rusm-wasm</code> backend; the <code>rusm</code> runtime composes them. The OTP layer is <em>all</em> of Phases 1–5, not just Phase 1.</p></blockquote><table tabindex="0"><thead><tr><th>Phase</th><th>Theme</th><th>Graduates to real data</th></tr></thead><tbody><tr><td><strong>0 ✅</strong></td><td>Observability + benchmark dashboard (synthetic)</td><td>—</td></tr><tr><td><strong>1 ✅</strong></td><td><strong>Process &amp; scheduler core</strong> — task + process table + abort-based lifecycle, pluggable body</td><td><strong>spawn-storm</strong> (live)</td></tr><tr><td><strong>2 ✅</strong></td><td><strong>Mailboxes &amp; message passing</strong> — per-process mailbox, <code>send</code>/<code>recv</code>, selective <code>recv_match</code></td><td><strong>ping-pong</strong> (live)</td></tr><tr><td><strong>3 ✅</strong></td><td><strong>Links, monitors, supervision, fault tolerance</strong> — exit reasons, <code>link</code>/<code>monitor</code>/<code>trap_exit</code>/<code>spawn_link</code>/<code>exit</code>, cascades</td><td><strong>fault-recovery</strong> (live)</td></tr><tr><td><strong>4 ✅</strong></td><td><strong>Process management</strong> — named registry, timers (<code>send_after</code>/<code>cancel</code>), graceful <code>shutdown</code></td><td>—</td></tr><tr><td><strong>5 ✅</strong></td><td><strong>Connectivity: TCP</strong> — <code>listen</code>/<code>connect</code>, process-per-connection (TLS folds into the Phase 9 secure cluster transport)</td><td><strong>connection-storm</strong> (live)</td></tr><tr><td><strong>6 ✅</strong></td><td><strong>Embed Wasmtime as the process backend</strong> — instance-per-process, host ABI, epoch preemption, pooling + CoW + <code>InstancePre</code>; fairness graduated to real Wasm</td><td><strong>fairness</strong> (live)</td></tr><tr><td><strong>7 ✅</strong></td><td><strong>Component hosting</strong> — the <strong>component model</strong> (WASI p2 + p3) via <code>bridges/</code>, a <code>rusm:runtime</code> WIT actor world (self/send/receive/list/info/kill/register), default-deny capability profiles + memory limits, process introspection, byte streams, and an app model (<code>rusm.toml [components.&lt;name&gt;]</code>, <code>rusm build</code>/<code>dev</code>). ~440k component spawns/s</td><td><strong>component-storm</strong> (live)</td></tr><tr><td><strong>8 ✅</strong></td><td><strong>Guest ergonomics</strong> — <strong><code>rusm-ts</code></strong> (TS/Bun → the rquickjs <strong>js-runner</strong>, no jco): <code>rusm build</code> bundles each TS component with Bun → <code>wasm/&lt;name&gt;.js</code>; <strong>service components</strong> export functions (RUSM runs the receive→dispatch→reply loop) and a <strong>worker</strong> exports <code>default</code>; the <strong>concealed typed client</strong> <code>spawn&lt;Svc&gt;(&quot;svc&quot;)</code> makes a cross-process call read like <code>await svc.method(...)</code> — plus <code>for await</code> <strong>streaming</strong> of generator handlers and <strong>callback</strong> args (a function stays in the caller, its invocations routed back) — over capability-gated spawn-from-guest (each component runs under its own declared profile); async <code>Process</code> API, binary messages + byte streams, all typed by the importable <strong><code>rusm-ts</code> npm package</strong> (<code>import { Process, spawn } from &quot;rusm-ts&quot;</code>). <strong><code>rusm-rs</code></strong> (the Rust twin): ergonomic <code>Pid</code>/<code>send</code>/<code>receive</code> (serde JSON, same wire as TS)/<code>spawn</code>/registry/<code>Stream</code> (wit-bindgen library/binary split) + a <code>#[rusm_rs::service]</code> macro → a dispatch loop + a typed <code>Client</code> with <strong>call/cast/streaming/callbacks</strong>. A Rust client and a TS service interoperate. Both get an in-guest <strong><code>Supervisor</code></strong> (one-for-one / one-for-all / rest-for-one over a <code>monitor</code> ABI), and <strong><code>rusm dev</code></strong> watches <code>./components</code> and rebuilds + reloads on edit.</td><td>—</td></tr><tr><td><strong>9 ✅</strong></td><td><strong>Distributed clusters + live attach</strong> — the Wasm-free <code>rusm-cluster</code> crate over <code>rusm-otp</code>: QUIC+TLS nodes, cross-node <code>send</code>, a gossiped <strong>global registry</strong> (<code>register_global</code>/<code>send_global</code>), <strong>remote spawn</strong> (named factories) and <strong>live attach</strong> (<code>remote_pids</code>) over one control-plane RPC. ~550k cross-node msgs/s, ~39µs p50 round-trip (loopback).</td><td><strong>distributed-fanout</strong> (live)</td></tr><tr><td><strong>10 ✅</strong></td><td><strong>Scale &amp; hardening</strong> — <em>not raw speed</em> (throughput/latency is already at the isolation-model ceiling: ~440k component spawns/s, ~21M msgs/s). <strong>On-demand instance tier</strong> (<code>WasmRuntime::with_overflow</code> — when the pooled cap is full, spawn from an on-demand engine so the live <em>Wasm</em>-process count is bounded by <strong>available memory</strong>, not a compile-time pool size); <strong>opt-in bounded mailboxes</strong> (<code>Runtime::with_mailbox_capacity</code> — shed <em>user</em> messages past capacity, system signals never shed); <strong>cluster security hardening</strong> (<code>ClusterCa</code> issues per-node certs, mutual TLS, foreign-CA peers rejected — replacing Phase 9&#39;s pre-shared cert); <strong>supervisor restart-intensity</strong> (windowed <code>{max_restarts, max_seconds}</code> in both in-guest supervisors). All with no spawn/message regression. See the <a href="/rusm/about/design-analysis">design analysis</a>.</td><td>—</td></tr><tr><td><strong>11 ✅</strong></td><td><strong>Standard-WASI surface &amp; serving</strong> — serve <strong>HTTP / WS / SSE</strong> from a component (<code>rusm serve</code> + <code>rusm.toml [[serve]]</code>); host <strong><code>wasi:http</code></strong> <em>inbound and outbound</em> (a capability-gated, streaming <code>fetch</code> for guests); run <strong>stock <code>wasi:cli/run</code> command components</strong> unchanged (<code>WasmRuntime::spawn_command</code>); support <a href="https://github.com/bytecodealliance/wstd" target="_blank" rel="noreferrer"><code>wstd</code></a> guests. The TS runner is hardened for real apps — <strong><code>crypto</code></strong> (getRandomValues/randomUUID over <code>wasi:random</code>) + a real streaming <code>fetch</code>/<code>Response</code>/<code>AbortController</code>. Byte streams work over a handle ABI (load-bearing for WS frame delivery + SSE bodies); a <strong>native p3-typed <code>stream&lt;u8&gt;</code></strong> signature is the one <strong>deferred</strong> refinement — cosmetic, since the handle ABI is functionally complete. <strong>0.3.0 then matured the whole serving surface</strong> (additive): a per-connection request <strong>context</strong> (route params → path-parameterised SSE), full WebSocket framing (text/close/ping-pong/subprotocols) with <strong>permessage-deflate</strong>, rich SSE events (<code>id</code>/<code>event</code>/<code>retry</code>) + Last-Event-ID resumption, per-listener resource &amp; CSWSH controls (<code>max_connections</code>/<code>max_message_size</code>/<code>allowed_origins</code>), gzip/deflate <strong>compression</strong>, and native <strong>TLS</strong> (<code>https</code>/<code>wss</code>).</td><td><strong>http/ws/sse</strong> (live)</td></tr><tr><td>12</td><td><strong>Edge &amp; cluster hardening</strong> — close the <a href="#security-audit-phase-12-scope">security-audit</a> exposures before RUSM faces <em>untrusted</em> traffic. <strong>Serve-path admission control</strong> (the remainder after 0.3.0 shipped <code>max_connections</code> / <code>max_message_size</code> / <code>allowed_origins</code>): a <strong>per-request wall-clock timeout</strong> + an HTTP <strong>request-body size cap</strong>, degrading to a graceful <code>503</code>/close (epoch preemption bounds only <em>CPU</em>; a slow-loris body is unbounded today). <strong>Bounded serve-path mailboxes by default</strong> so a fast peer can&#39;t OOM a slow WS handler (the opt-in <code>with_mailbox_capacity</code> exists; the serve path doesn&#39;t set it). <strong>Cluster trust hardening</strong>: signed <code>name→node</code> ownership so a <em>mutually-authenticated but malicious</em> peer can&#39;t poison the gossiped global registry (mTLS authenticates the connection, not the gossip payload), plus poison-resistant locking in the <code>rusm-cluster</code> control loop (replace <code>lock().unwrap()</code>). The sandbox, capability model, trap isolation, supervision, and mailbox load-shedding are <strong>already sound</strong> — these are network-edge and peer-trust gaps, not sandbox breaks.</td><td>—</td></tr></tbody></table><h2 id="what-s-shipped-so-far-phases-0–11" tabindex="-1">What&#39;s shipped so far (Phases 0–11) <a class="header-anchor" href="#what-s-shipped-so-far-phases-0–11" aria-label="Permalink to &quot;What&#39;s shipped so far (Phases 0–11)&quot;">​</a></h2><ul><li><strong>Phase 0 — observability &amp; harness:</strong> <code>rusm-metrics</code>, <code>rusm-observer</code>, <code>rusm-bench</code> (+ WebSocket server), <code>rusm-cli</code>, the React dashboard, and this docs/VitePress site.</li><li><strong>Phases 1–5 — the Wasm-free OTP core (<code>rusm-otp</code>):</strong> process &amp; scheduler core, mailboxes &amp; message passing, links/monitors/supervision, the named registry + timers + graceful shutdown, and TCP (process-per-connection).</li><li><strong>Phase 6 — Wasmtime backend (<code>rusm-wasm</code>):</strong> instance-per-process, host ABI, epoch preemption, pooling + CoW + <code>InstancePre</code>.</li><li><strong>Phase 7 — component hosting:</strong> the component model (WASI <strong>p2 + p3</strong>) via <code>bridges/{wasip1,wasip2,wasip3}</code>, the <code>rusm:runtime</code> WIT actor world, default-deny capabilities, cross-process byte streaming, and the <code>rusm.toml</code> app model.</li><li><strong>Phase 8 — guest ergonomics:</strong> <code>rusm-ts</code> (TS/Bun → js-runner) + <code>rusm-rs</code> (the Rust twin), service components + a concealed typed client (call/cast/streaming/ callbacks), spawn-from-guest, an in-guest <code>Supervisor</code>, and <code>rusm dev</code> reload.</li><li><strong>Phase 9 — distributed clusters (<code>rusm-cluster</code>):</strong> QUIC+TLS nodes, cross-node <code>send</code>, a gossiped global registry, remote spawn, and live attach — over <code>rusm-otp</code>, Wasm-free.</li><li><strong>Phase 10 — scale &amp; hardening:</strong> on-demand instance tier (lift the pooled cap), opt-in bounded mailboxes (overload load-shed), per-node certs under a cluster CA <ul><li>mutual TLS, and windowed supervisor restart-intensity — no spawn/message regression.</li></ul></li><li><strong>Phase 11 — serving &amp; standard-WASI surface</strong> (functionally complete; one refinement deferred): run a component as a high-throughput <strong>HTTP / WS / SSE</strong> server — HTTP/SSE instance-per-request, WS one process per connection, from Rust, TypeScript, and Go guests. <strong><code>rusm serve</code></strong> hosts <code>rusm.toml [[serve]]</code> listeners with declarative <code>[serve.routes]</code>; <strong><code>rusm new</code></strong> scaffolds a ready-to-serve app. The standard-WASI surface is closed too: stock <code>wasi:cli/run</code> components, a capability-gated outbound <code>fetch</code>, and <code>crypto</code>. <strong>0.3.0</strong> matured it — request context, full WS framing + permessage-deflate, rich SSE + resumption, resource/CSWSH controls, compression, and native <strong>TLS</strong>. Fair out-of-process numbers (<code>rusm-loadtest</code>, loopback): HTTP ~46k req/s, WS ~146k round-trips/s, SSE ~609k events/s. The one deferred item is a native p3-typed <code>stream&lt;u8&gt;</code> signature (cosmetic). Full story: <a href="/rusm/phases/phase-11-serving">Phase 11</a> and <a href="/rusm/deep-dive/serving-http-ws-and-sse">serving HTTP/WS/SSE</a>.</li></ul><p><strong>Across all phases:</strong></p><ul><li><strong>Twenty-one live dashboard benchmarks</strong> — <em>every</em> scenario now runs on real data: the ten core engines (spawn-storm, ping-pong, fault-recovery, connection-storm, connection-scale, fairness, module-storm, component-storm, stream-pipe, distributed-fanout), six co-resident serving demos (<code>http-throughput</code>, <code>ws-echo</code>, <code>sse-fanout</code> and their <code>*-ts</code> twins), three platform-primitive scenarios (<code>kv-storm</code> durable read-modify-writes over redb, <code>pubsub-fanout</code> 1→N broadcast, <code>crypto-ops</code> <code>crypto.subtle</code> from a TS guest), and two extension-primitive scenarios (<code>custom-bridge</code> a guest calling an app&#39;s native bridge, <code>dynamic-wasm</code> a runtime-loaded compiled component through the compile cache) + the standalone <code>cluster_fanout</code> benchmark. The fair, credible serving headline numbers are still measured by <code>rusm-loadtest</code> (out-of-process, vs a live <code>rusm serve</code> port).</li><li>TDD throughout; coverage ≥98% (mostly 100%); <code>cargo fmt</code> + Prettier clean.</li></ul><h2 id="planned" tabindex="-1">Planned <a class="header-anchor" href="#planned" aria-label="Permalink to &quot;Planned&quot;">​</a></h2><h3 id="security-audit-phase-12-scope" tabindex="-1">Security audit (Phase 12 scope) <a class="header-anchor" href="#security-audit-phase-12-scope" aria-label="Permalink to &quot;Security audit (Phase 12 scope)&quot;">​</a></h3><p>A security/robustness review found the <strong>core sound</strong>: the capability sandbox is true default-deny, spawn-from-guest is capability-gated (a node-registered component runs under its own manifest-declared profile; a guest can&#39;t fabricate capabilities the operator never granted), a guest trap is isolated to that one process (no scheduler/runtime crash, no cascade), TS guests are sandboxed exactly like Rust ones (rquickjs compiled to <code>wasm32-wasip2</code>), the <code>rusm-otp</code> core is panic-isolated (sharded <code>DashMap</code>, <code>Drop</code>-guard reaping), cluster transport enforces <strong>mutual TLS</strong> (foreign-CA peers rejected), remote spawn is gated to pre-registered factories, supervisor restart-intensity is windowed, and bounded mailboxes shed only <em>user</em> messages (system/exit signals are never dropped). <strong>No sandbox escapes or privilege escalation.</strong></p><p>The gaps are <strong>operational (DoS hardening) and trust-model</strong>, not architectural — and become <strong>Phase 12</strong>:</p><ul><li><strong>Serve-path admission control (medium).</strong> 0.3.0 added a concurrency bound (<code>max_connections</code>, every serving path), a WS message-size cap (<code>max_message_size</code>), and a CSWSH origin allow-list (<code>allowed_origins</code>). What remains: an <strong>HTTP request-body cap</strong> and a <strong>per-request wall-clock timeout</strong> (epoch only bounds CPU; a slow-loris body is still unbounded), degrading to a graceful <code>503</code>. Without the cap the serve path degrades <em>gracefully</em> at the pool cap (verified, <strong>not</strong> a panic), but with the on-demand overflow tier a flood is memory-bounded, not count-bounded.</li><li><strong>Default-unbounded serve mailboxes (medium).</strong> Erlang-compatible, but a fast peer flooding a slow WS handler is an OOM vector; bound the serve path&#39;s mailboxes by default.</li><li><strong>Cluster peer-trust (medium).</strong> mTLS authenticates the <em>connection</em>; a malicious <em>authenticated</em> peer can still advertise false <code>name→node</code> ownership in the gossiped global registry (silent misrouting). Sign ownership; and replace <code>lock().unwrap()</code> in the control loop with poison-resistant locking.</li></ul><p>These are the items to land before exposing <code>rusm serve</code> to untrusted traffic.</p><p>See the per-phase deep dives under <a href="/rusm/phases/phase-00-foundation"><code>phases/</code></a>, and the <a href="/rusm/about/rusm-vs-lunatic">RUSM vs Lunatic comparison</a> for the per-phase &quot;borrow vs beat&quot; efficiency playbook we update as each gap closes.</p>',16)])])}const u=o(n,[["render",a]]);export{m as __pageData,u as default};
