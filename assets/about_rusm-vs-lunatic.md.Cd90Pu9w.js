import{_ as t,o,c as a,a2 as s}from"./chunks/framework.uw_ZoRon.js";const m=JSON.parse('{"title":"RUSM vs Lunatic — comparison & efficiency playbook","description":"","frontmatter":{},"headers":[],"relativePath":"about/rusm-vs-lunatic.md","filePath":"about/rusm-vs-lunatic.md"}'),r={name:"about/rusm-vs-lunatic.md"};function n(d,e,i,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="rusm-vs-lunatic-—-comparison-efficiency-playbook" tabindex="-1">RUSM vs Lunatic — comparison &amp; efficiency playbook <a class="header-anchor" href="#rusm-vs-lunatic-—-comparison-efficiency-playbook" aria-label="Permalink to &quot;RUSM vs Lunatic — comparison &amp; efficiency playbook&quot;">​</a></h1><p>Lunatic is RUSM&#39;s closest peer — the project that first made &quot;a Wasm instance is a process&quot; real, with supervisors and a distributed registry. That makes it the most useful mirror RUSM has: studying it carefully shows both the ideas worth being inspired by and the places where a fresh implementation, on a modern Wasmtime, can do better. This page is two things at once — an honest, phase-by-phase comparison of where the two runtimes stand today, and a working <strong>efficiency playbook</strong>: for each phase, the smart Lunatic technique to borrow (with file evidence), why it helps, and where RUSM deliberately aims to be faster, leaner, or more stable.</p><blockquote><p><strong>Living document.</strong> RUSM is built in phases; Lunatic is a complete (but dormant, v0.13.0 / May 2023) runtime. We keep this doc current as each RUSM phase closes a gap. It exists to (a) honestly track where RUSM stands, (b) capture the smart Lunatic techniques worth being <em>inspired</em> by (never copied), and (c) record where RUSM deliberately aims to be <strong>faster, leaner, and more stable</strong>.</p><p>Source studied: <code>~/Sources/lunatic</code> (Wasmtime 8). File references are to that tree.</p></blockquote><h2 id="how-to-read-this" tabindex="-1">How to read this <a class="header-anchor" href="#how-to-read-this" aria-label="Permalink to &quot;How to read this&quot;">​</a></h2><p>This is not apples-to-apples. RUSM has <strong>Phases 0–11 functionally complete</strong> (Phase 12, hardening, is planned), while Lunatic is a complete but <strong>dormant</strong> runtime. In RUSM today: the OTP core spawns, messages, supervises, manages, and connects real processes; the <code>rusm-wasm</code> backend runs them as real <strong>Wasm instances</strong> behind three bridges (wasip1 core modules, wasip2 components, wasip3 <code>@0.3.0</code>) with default-deny capabilities, instance-per-process, and pooling + CoW + epoch; the guest crates (<code>rusm-rs</code> / <code>rusm-ts</code> / <code>rusm-go</code>), the QUIC+TLS cluster, and HTTP/WS/SSE serving are all built. All <strong>twenty-one</strong> dashboard scenarios run on real data — including <strong>fairness</strong> (Wasm spinners saturate every core, yet bystanders keep progressing) and <strong>distributed-fanout</strong> (real cross-node messaging over QUIC+TLS). The lasting value of this page is the <strong>efficiency playbook</strong> below.</p><blockquote><h3 id="does-rusm-handle-lightweight-processes-as-efficiently-as-lunatic-—-today" tabindex="-1">Does RUSM handle lightweight processes as efficiently as Lunatic — today? <a class="header-anchor" href="#does-rusm-handle-lightweight-processes-as-efficiently-as-lunatic-—-today" aria-label="Permalink to &quot;Does RUSM handle lightweight processes as efficiently as Lunatic — today?&quot;">​</a></h3><p><strong>For the actor model — yes, and in places better</strong> (one channel per process vs two, free abort-handle cancellation, a sharded registry, Tokio-wheel timers; ~2.4M spawns/sec, ~21M messages/sec, ~285k restarts/sec measured live). <strong>For Wasm execution — RUSM now hosts the <em>component model</em></strong> (<code>rusm-wasm</code>), which Lunatic does <strong>not</strong> do at all (it runs only core modules with its own ABI): instance-per-process with a pooling allocator, copy-on-write init, <code>InstancePre</code>, a precomputed export index, and epoch preemption — <strong>~440k component spawns/sec</strong> live (component-storm). Lunatic ships on-demand allocation + fuel; RUSM&#39;s spawn path is ahead by design.</p><p><strong>The direct head-to-head</strong> is the <strong>module-storm</strong> scenario: RUSM spawns the <em>same artifact Lunatic hosts</em> — wasip1 core modules — at <strong>~475k spawns/sec</strong> live, recycling pooled instances and preempting with epochs (vs Lunatic&#39;s on-demand allocation + per-instruction fuel). Telling detail: that&#39;s ~the same as a wasip2 component (~440k/sec) — <strong>the component model is nearly free over a raw core module</strong> on RUSM&#39;s pooled path. The only big step is to a bare task (~2.4M/sec); that ~5x is the price of real Wasm memory isolation, paid once whether you run a core module or a component.</p><p>The <strong>fairness</strong> scenario runs on real Wasm — spinners saturate every core, yet bystanders progress at ~50M+ ops/sec (past 400M on free cores), proving epoch preemption live. What remains is a true head-to-head against Lunatic (see the question at the end).</p></blockquote><h2 id="snapshot" tabindex="-1">Snapshot <a class="header-anchor" href="#snapshot" aria-label="Permalink to &quot;Snapshot&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>RUSM (today)</th><th>Lunatic</th></tr></thead><tbody><tr><td>Status</td><td>Active, Phases 0–11 complete (12 planned)</td><td>Dormant since 2023 (v0.13.0)</td></tr><tr><td>Rust LOC</td><td>~5,350 (6 crates) + ~790 TS</td><td>~15,150 (20 crates)</td></tr><tr><td>Tests</td><td>~164 Rust + 21 TS, ~99% cov</td><td>~26 test annotations</td></tr><tr><td>Wasmtime</td><td>v45 (instance-per-process)</td><td>v8 (2023)</td></tr><tr><td>Guest target</td><td>components (<code>wasm32-wasip2</code>, WASI p2/p3) + core modules (<code>wasm32-wasip1</code>, WASI p1)</td><td><code>wasm32-wasi</code> (preview1)</td></tr><tr><td>License</td><td>MIT</td><td>MIT + Apache-2.0</td></tr></tbody></table><h2 id="capability-matrix" tabindex="-1">Capability matrix <a class="header-anchor" href="#capability-matrix" aria-label="Permalink to &quot;Capability matrix&quot;">​</a></h2><p>Implementation: ✅ done · ⚠️ partial/synthetic · ❌ not yet · 🅛 Lunatic-only · 🅡 RUSM-only Perf/efficiency vs Lunatic: ✅ on par · 🔥 ahead by design¹ · — not built yet</p><p>This mirrors the <a href="/rusm/about/roadmap">roadmap</a> <strong>phase-for-phase</strong> — one row per phase, same themes, same order.</p><table tabindex="0"><thead><tr><th>Phase</th><th>Capability</th><th style="text-align:center;">RUSM</th><th>Lunatic</th><th>On par? (perf/efficiency)</th></tr></thead><tbody><tr><td>1 ✅</td><td>Process &amp; scheduler core</td><td style="text-align:center;">✅ done (<code>rusm-otp</code>)</td><td>✅ <code>WasmProcess</code></td><td>✅ on par</td></tr><tr><td>2 ✅</td><td>Mailboxes &amp; message passing</td><td style="text-align:center;">✅ done (one channel + selective receive)</td><td>✅ selective-receive</td><td>🔥 ahead</td></tr><tr><td>3 ✅</td><td>Links, monitors, supervision, fault tolerance</td><td style="text-align:center;">✅ done (links/monitors/trap/exit)</td><td>✅ <code>Signal</code> enum</td><td>🔥 ahead</td></tr><tr><td>4 ✅</td><td>Process management (registry, timers, lifecycle)</td><td style="text-align:center;">✅ done (sharded registry, Tokio timers)</td><td>✅</td><td>🔥 ahead</td></tr><tr><td>5 ✅</td><td>Connectivity — TCP</td><td style="text-align:center;">✅ done (TCP, process-per-conn; TLS shipped in P9 via QUIC)</td><td>✅ TCP/UDP/DNS/TLS</td><td>✅ on par</td></tr><tr><td>6 ✅</td><td>Wasmtime backend (instance-per-process, preemption)</td><td style="text-align:center;">✅ done (pooling+CoW+epoch; fairness live)</td><td>✅ (fuel)</td><td>🔥 ahead by design</td></tr><tr><td>7 ✅</td><td><strong>Component hosting</strong> (component model, WASI p2 + p3, capabilities, actor WIT ABI, app model)</td><td style="text-align:center;">✅ done (~440k component spawns/s; default-deny caps + memory limits; component-storm live)</td><td>❌ <strong>no component-model host</strong> (core modules only)</td><td>🔥 <strong>ahead — an axis Lunatic lacks</strong></td></tr><tr><td>↳ 7 ✅</td><td>…wasip1 bridge (full WASI + raw actor ABI + byte streams) + wasip3 interfaces on the component linker — <em>part of Phase 7, not a separate phase</em></td><td style="text-align:center;">✅ done</td><td>✅ wasip1</td><td>🔥 ahead (p3 + components)</td></tr><tr><td>8 ✅</td><td>Guest crate</td><td style="text-align:center;">✅ <code>rusm-rs</code> + <code>rusm-ts</code> (service macro / typed client, call/cast/stream/callbacks)</td><td>🅛 <code>lunatic-rs</code> (Rust only)</td><td>🔥 ahead — TS <em>and</em> Rust guests, one wire</td></tr><tr><td>9 ✅</td><td>Distributed clusters + live attach</td><td style="text-align:center;">✅ <code>rusm-cluster</code> (QUIC+TLS, cross-node send, gossiped global registry, remote spawn, live attach)</td><td>✅ (QUIC + distributed registry)</td><td>✅ at parity — secure cluster + global registry, one persistent conn/node, message-per-stream (no custom congestion layer)</td></tr><tr><td>10 ✅</td><td>Scale &amp; hardening</td><td style="text-align:center;">✅ on-demand instance tier, bounded mailboxes, mutual-TLS cluster CA, windowed restart-intensity</td><td>⚠️ OnDemand + fuel</td><td>🔥 ahead — overflow tier <em>on top of</em> pooling, + secure cluster</td></tr><tr><td>11 ✅</td><td><strong>Serving</strong> (HTTP / WS / SSE from a component)</td><td style="text-align:center;">✅ engine built+measured, <strong>from Rust, TS, and Go guests</strong> — <code>http_server</code> (instance-per-request <code>wasi:http</code>), <code>ws_server</code> (one sandboxed component process per WS connection), SSE streaming body. <strong><code>rusm serve</code></strong> hosts <code>rusm.toml [[serve]]</code> entries on real ports; <strong><code>rusm new</code></strong> scaffolds an app. Six <strong>co-resident live demos</strong> on the dashboard (<code>http-throughput</code>/<code>ws-echo</code>/<code>sse-fanout</code> + <code>*-ts</code> twins); the <strong>fair headline numbers</strong> come <strong>out-of-process</strong> from <code>rusm-loadtest</code> (vs a live port): HTTP ~46k req/s (0% errors), WS ~146k round-trips/s (256 held), SSE ~609k events/s (256 held), ~34k sandboxed-process-per-connection WS establishments/s (<code>conn</code> mode). 0.3.0 matured it: connection context (route params), WS framing + permessage-deflate, rich SSE + Last-Event-ID, resource/CSWSH controls, compression, and native <strong>TLS</strong> (<code>https</code>/<code>wss</code>).</td><td>❌ <strong>no <code>wasi:http</code> host</strong> (core modules only)</td><td>🔥 <strong>ahead — an axis Lunatic lacks</strong></td></tr><tr><td>12 ⏳</td><td><strong>Edge &amp; cluster hardening</strong> (planned)</td><td style="text-align:center;">the serve-path admission-control remainder (request-body cap + per-request timeout → graceful <code>503</code>; concurrency/message caps + CSWSH shipped in 0.3.0), default-bounded serve mailboxes, signed <code>name→node</code> gossip ownership + poison-resistant locking</td><td>⚠️ partial</td><td>— closing network-edge &amp; peer-trust gaps</td></tr><tr><td>—</td><td>SQLite host API</td><td style="text-align:center;">❌</td><td>🅛</td><td>— n/a</td></tr></tbody></table><blockquote><p>¹ The perf column is an <strong>architectural</strong> assessment, not a head-to-head benchmark — and Phases 1–5 run <strong>native</strong> Rust bodies, so they compare the OTP/host machinery, <em>not</em> Wasm execution. The true lightweight-process efficiency race is <strong>Phase 6</strong>, when guests become real Wasm instances.</p></blockquote><p><strong>Why each verdict (perf/efficiency):</strong></p><ul><li><strong>1 — on par.</strong> Identical model: one process = one Tokio task. RUSM&#39;s native spawn sustains ~2.4M/s; Lunatic&#39;s per-spawn also instantiates a Wasm module, so a fair head-to-head waits for Phase 6.</li><li><strong>2 — ahead.</strong> RUSM keeps <strong>one</strong> channel per process (the mailbox); Lunatic keeps two (signal + message) and double-handles each message (mpsc → <code>Mutex&lt;VecDeque&gt;</code>). Kill rides a free abort handle — less memory, one fewer queue per process.</li><li><strong>3 — ahead.</strong> Exit signals ride the mailbox (no separate signal channel to multiplex), and a crash is captured via <code>std::thread::panicking()</code> with <strong>no <code>catch_unwind</code></strong> per-poll cost.</li><li><strong>4 — ahead.</strong> The registry is a sharded <code>DashMap</code> (name lookups never take a global lock, unlike Lunatic&#39;s single <code>RwLock&lt;HashMap&gt;</code>); timers ride Tokio&#39;s hierarchical wheel instead of a hand-rolled <code>BinaryHeap</code> + one timer-service task.</li><li><strong>5 — on par.</strong> Both are process-per-connection; the connection <em>rate</em> is the OS kernel <code>connect</code>/<code>accept</code> ceiling (identical for both), and minting a process per connection is ~free on both (RUSM spawns ~100× faster than the loopback hands out sockets).</li><li><strong>6/7 — ahead by design.</strong> A <strong>pooling allocator</strong> + <strong>copy-on-write</strong> memory init + a per-module <strong><code>InstancePre</code></strong> + a <strong>precomputed export index</strong> sustain <strong>~440k component instance-per-process spawns/s</strong>, far ahead of a naive on-demand allocator, and <strong>epoch</strong> preemption (bumped on a dedicated thread) keeps bystanders at ~50M+ ops/sec (past 400M on free cores) even with a tight-loop guest pinning every core. Lunatic ships on-demand allocation + fuel (and no component host), so RUSM is ahead on both the spawn path and preemption overhead by design — a true head-to-head benchmark is the remaining validation.</li></ul><p>Already shipped in Phase 0 — where RUSM already leads Lunatic:</p><table tabindex="0"><thead><tr><th>Capability</th><th style="text-align:center;">RUSM</th><th style="text-align:center;">Lunatic</th></tr></thead><tbody><tr><td>HdrHistogram latency metrics</td><td style="text-align:center;">✅✅</td><td style="text-align:center;">⚠️ passthrough facade</td></tr><tr><td>Live observer + REPL attach</td><td style="text-align:center;">✅✅</td><td style="text-align:center;">❌</td></tr><tr><td>Web dashboard</td><td style="text-align:center;">✅✅</td><td style="text-align:center;">❌</td></tr><tr><td>Enforced ≥98% coverage + docs site</td><td style="text-align:center;">✅✅</td><td style="text-align:center;">❌</td></tr></tbody></table><hr><h1 id="efficiency-playbook-—-phase-by-phase" tabindex="-1">Efficiency playbook — phase by phase <a class="header-anchor" href="#efficiency-playbook-—-phase-by-phase" aria-label="Permalink to &quot;Efficiency playbook — phase by phase&quot;">​</a></h1><p>Same order as the roadmap and the matrix above. For each phase: the smart Lunatic techniques to <strong>borrow</strong> (with file evidence in <code>~/Sources/lunatic</code>), why they help, and where RUSM <strong>beats</strong> them. (Borrow ≠ copy — understand, then write our own.)</p><h2 id="phase-1-—-process-scheduler-core-✅" tabindex="-1">Phase 1 — Process &amp; scheduler core ✅ <a class="header-anchor" href="#phase-1-—-process-scheduler-core-✅" aria-label="Permalink to &quot;Phase 1 — Process &amp; scheduler core ✅&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Borrow from Lunatic</th><th>Why it helps</th><th>RUSM plan</th></tr></thead><tbody><tr><td>Biased <code>tokio::select!</code> loop, signals before the body — <code>lunatic-process/src/lib.rs</code></td><td>deterministic signal priority, no starvation, cancellation-safe</td><td><strong>Ahead (Phase 2):</strong> kill now rides a <code>futures</code> abort handle, so there&#39;s no select loop and no control channel at all</td></tr><tr><td>Single <code>Signal</code> enum over one mpsc channel — <code>lunatic-process/src/lib.rs</code></td><td>one channel for messages <em>and</em> control — uniform</td><td><strong>Ahead:</strong> RUSM keeps one channel for <em>messages only</em>; control needs none, so we removed the <code>Signal</code> type entirely</td></tr><tr><td><code>HashMapId&lt;T&gt;</code> id→resource table — <code>crates/hash-map-id</code></td><td>one uniform resource table everywhere</td><td>Adopt the <em>pattern</em>; <strong>beat:</strong> a slotmap / generational-index arena (array-indexed, no hashing, safe id reuse) instead of <code>HashMap&lt;u64,T&gt;</code></td></tr><tr><td>Unbounded signal mailbox (<code>UnboundedSender</code>) — <code>lunatic-process/src/lib.rs</code></td><td>Erlang-style, but unbounded → flood/memory risk</td><td>RUSM&#39;s mailbox is unbounded too (Erlang-style); <strong>bounded + observable mailbox depth</strong> is a later hardening option</td></tr></tbody></table><h2 id="phase-2-—-mailboxes-message-passing-✅" tabindex="-1">Phase 2 — Mailboxes &amp; message passing ✅ <a class="header-anchor" href="#phase-2-—-mailboxes-message-passing-✅" aria-label="Permalink to &quot;Phase 2 — Mailboxes &amp; message passing ✅&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Borrow from Lunatic</th><th>Why it helps</th><th>RUSM status</th></tr></thead><tbody><tr><td>Cancellation-safe selective receive by tag (waker + found-on-cancel re-queue) — <code>mailbox.rs:39-169</code></td><td>safe in <code>select!</code>, no lost messages</td><td><strong>✅ Done</strong> — <code>Context::recv_match</code> scans a save queue then the channel, leaving non-matches in arrival order (own code + tests)</td></tr><tr><td>⚠️ Data messages share the single <code>Signal</code> mpsc with control</td><td>a message flood can delay <code>Kill</code>/<code>Link</code> handling (FIFO within one channel)</td><td><strong>✅ Ahead</strong> — control (kill) rides a free abort handle, so messages have the mailbox entirely to themselves; <em>zero</em> control channels vs Lunatic&#39;s shared signal mpsc</td></tr><tr><td>⚠️ Two queues per message (signal mpsc → <code>Mutex&lt;VecDeque&gt;</code>) + a <code>Vec&lt;u8&gt;</code> per message</td><td>double handoff + an allocation per message</td><td><strong>✅ Ahead</strong> — one mailbox queue per process, no double handoff; small-message inlining (smallvec) and buffer pooling remain a later option</td></tr><tr><td><code>DataMessage{buffer, resources: Vec&lt;Arc&lt;Resource&gt;&gt;}</code> — resources moved by <code>Arc</code>, only bytes copied — <code>message.rs:68-103</code></td><td>zero-copy handoff of sockets/modules</td><td><strong>Planned</strong> — Phase 2 carries opaque bytes (pids encoded inline); first-class typed resources land with the host ABI (Phase 6)</td></tr><tr><td>Address peers via held handles — no global-table lookup on <code>send</code> — <code>lunatic-process/src/lib.rs</code></td><td>the send hot path never locks a global table</td><td><strong>Partial</strong> — <code>send</code> goes through a <em>sharded</em> <code>DashMap</code> (no global lock, unlike our old <code>Mutex&lt;HashMap&gt;</code>); pure handle addressing is a later option</td></tr></tbody></table><h2 id="phase-3-—-links-supervision-fault-tolerance-✅" tabindex="-1">Phase 3 — Links, supervision, fault tolerance ✅ <a class="header-anchor" href="#phase-3-—-links-supervision-fault-tolerance-✅" aria-label="Permalink to &quot;Phase 3 — Links, supervision, fault tolerance ✅&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Borrow from Lunatic</th><th>Why it helps</th><th>RUSM status</th></tr></thead><tbody><tr><td><code>Signal::{Link,Monitor,LinkDied}</code> + <code>die_when_link_dies</code> — <code>lunatic-process/src/lib.rs</code></td><td>unified, configurable supervision</td><td><strong>✅ Done, ahead</strong> — <code>link</code>/<code>monitor</code>/<code>trap_exit</code>/<code>spawn_link</code>/<code>exit</code>, but exit signals ride the <em>mailbox</em> (a <code>Received</code> enum) and kill rides the abort handle, so there&#39;s still <strong>no separate signal channel</strong> to multiplex</td></tr><tr><td>trap → <code>ResultValue::Failed</code> → <code>LinkDied</code> propagation — <code>runtimes/wasmtime.rs</code></td><td>a crash notifies linked peers</td><td><strong>✅ Done</strong> — a crash is caught via <code>std::thread::panicking()</code> in the teardown guard (no <code>catch_unwind</code>, no per-poll cost); the abnormal reason cascades down links and is <em>staged</em> so a cascaded peer reports the original reason, not a bare kill</td></tr></tbody></table><h2 id="phase-4-—-process-management-✅" tabindex="-1">Phase 4 — Process management ✅ <a class="header-anchor" href="#phase-4-—-process-management-✅" aria-label="Permalink to &quot;Phase 4 — Process management ✅&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Borrow from Lunatic</th><th>Why it helps</th><th>RUSM status</th></tr></thead><tbody><tr><td>Named registry <code>Arc&lt;RwLock&lt;HashMap&gt;&gt;</code> — <code>lunatic-registry-api</code></td><td>async-safe name → pid</td><td><strong>✅ Done, ahead</strong> — a sharded <code>DashMap</code> registry (name lookups never take a global lock), with names auto-released on process exit</td></tr><tr><td>Timers: <code>BinaryHeap</code> + <code>HashMapId</code> (O(log n) cancel) — <code>lunatic-timer-api</code></td><td>cheap cancellation of many timers</td><td><strong>✅ Done, simpler</strong> — <code>send_after</code> rides Tokio&#39;s hierarchical timer wheel and cancellation is a free <code>AbortHandle</code>, so there&#39;s no hand-rolled heap and no single timer-service bottleneck</td></tr></tbody></table><h2 id="phase-5-—-connectivity-tcp-✅" tabindex="-1">Phase 5 — Connectivity (TCP) ✅ <a class="header-anchor" href="#phase-5-—-connectivity-tcp-✅" aria-label="Permalink to &quot;Phase 5 — Connectivity (TCP) ✅&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Borrow from Lunatic</th><th>Why it helps</th><th>RUSM status</th></tr></thead><tbody><tr><td>Process-per-connection accept loop — <code>lunatic-networking-api</code></td><td>a slow/crashing connection can&#39;t affect the others</td><td><strong>✅ Done</strong> — <code>Runtime::listen</code> spawns one rusm-otp process per accepted socket; the connection ceiling is the OS (fds, ephemeral ports, TIME_WAIT), not RUSM, since spawning is ~free (the spawn storm does 2.4M/s)</td></tr><tr><td>TCP owned read/write halves + per-conn timeouts in <code>HashMapId</code> — <code>lunatic-networking-api/src/lib.rs:71</code></td><td>concurrent reader+writer without locking the stream</td><td><strong>Deferred</strong> — native handlers own the whole <code>TcpStream</code>; split halves / per-conn timeouts arrive with the guest host ABI (Phase 6)</td></tr><tr><td>TLS via <code>tokio-rustls</code> + <code>webpki-roots</code> — <code>tls_tcp.rs:392</code></td><td>secure transport</td><td><strong>Moved to Phase 9</strong> — TLS&#39;s real home is the secure cluster transport (QUIC + TLS); bolting it onto the loopback storm would only tank throughput</td></tr></tbody></table><h2 id="phase-6-—-wasmtime-backend-✅-←-the-biggest-efficiency-win" tabindex="-1">Phase 6 — Wasmtime backend ✅ ← the biggest efficiency win <a class="header-anchor" href="#phase-6-—-wasmtime-backend-✅-←-the-biggest-efficiency-win" aria-label="Permalink to &quot;Phase 6 — Wasmtime backend ✅  ← the biggest efficiency win&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Borrow from Lunatic</th><th>Why it helps</th><th>RUSM status</th></tr></thead><tbody><tr><td><code>InstancePre</code> (imports type-checked once) + Arc&#39;d <code>Module</code> + async — <code>runtimes/wasmtime.rs:34,63,163</code></td><td>fast instantiation, compile-once</td><td><strong>✅ Done</strong> — the linker is built once and each module&#39;s imports resolve once into an <code>InstancePre</code>; a spawn skips import resolution (and a precomputed export index skips the by-name lookup) — part of the path to ~440k component spawns/s</td></tr><tr><td><code>InstanceAllocationStrategy::OnDemand</code> — <code>runtimes/wasmtime.rs:173</code></td><td>fresh memory per instance → slower, heavier spawn</td><td><strong>✅ Ahead</strong> — pooling allocator (pre-reserved slabs) → spawns reuse slots, no mmap (a large multiple of on-demand allocation)</td></tr><tr><td><code>static_memory_forced(true)</code> on v8 (no CoW) — <code>runtimes/wasmtime.rs:175</code></td><td>static memories, but no copy-on-write</td><td><strong>✅ Ahead</strong> — <code>memory_init_cow</code>: a fresh instance shares the module image until first write, so init is near-free</td></tr><tr><td>Preemption via <strong>fuel</strong> (<code>consume_fuel</code>, <code>out_of_fuel_async_yield</code>) — <code>runtimes/wasmtime.rs:166,56</code></td><td>works, but per-unit accounting overhead</td><td><strong>✅ Ahead</strong> — epoch interruption: a periodic atomic bump, ≈ near-zero steady-state; an infinite-loop guest still yields and stays killable</td></tr></tbody></table><p><strong>Why this phase matters most:</strong> pooling + CoW + epoch + <code>InstancePre</code> + a precomputed export index are exactly the levers for cheap instance-per-process. They&#39;re in (<code>rusm-wasm</code>), giving ~440k component spawns/s, and the fairness scenario proves epoch preemption live. Lunatic (Wasmtime 8, on-demand, fuel, core-modules-only) predates easy access to them — the remaining work is a true head-to-head benchmark to put numbers on the delta.</p><h2 id="phase-7-—-wasi-per-process-sandbox" tabindex="-1">Phase 7 — WASI + per-process sandbox <a class="header-anchor" href="#phase-7-—-wasi-per-process-sandbox" aria-label="Permalink to &quot;Phase 7 — WASI + per-process sandbox&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Borrow from Lunatic</th><th>Why it helps</th><th>RUSM plan</th></tr></thead><tbody><tr><td>WASI preopens (scoped fs), isolated per-process stdio — <code>lunatic-wasi-api</code></td><td>fine-grained filesystem sandbox</td><td>Adopt; <strong>beat:</strong> finer memory/fuel limits per process</td></tr><tr><td>stdout capture as a <code>WasiFile</code> — <code>lunatic-stdout-capture</code></td><td>isolated, inspectable output</td><td>Adopt — feeds the observer/attach</td></tr></tbody></table><h2 id="phase-8-—-guest-crate" tabindex="-1">Phase 8 — Guest crate <a class="header-anchor" href="#phase-8-—-guest-crate" aria-label="Permalink to &quot;Phase 8 — Guest crate&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Borrow from Lunatic</th><th>Why it helps</th><th>RUSM plan</th></tr></thead><tbody><tr><td><code>lunatic-rs</code> API shape — spawn / <code>Mailbox</code> / <code>AbstractProcess</code> / <code>Supervisor</code> (separate repo)</td><td>a familiar, ergonomic guest API</td><td>✅ <code>rusm-rs</code> <em>and</em> <code>rusm-ts</code> ship <code>Pid</code>/<code>send</code>/<code>receive</code>/<code>spawn</code>/<code>Stream</code>, a <code>#[service]</code> macro (typed <code>Client</code>), and an in-guest <code>Supervisor</code> (one-for-one / one-for-all / rest-for-one)</td></tr></tbody></table><h2 id="phase-9-—-distributed-clusters-live-attach" tabindex="-1">Phase 9 — Distributed clusters + live attach <a class="header-anchor" href="#phase-9-—-distributed-clusters-live-attach" aria-label="Permalink to &quot;Phase 9 — Distributed clusters + live attach&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Borrow from Lunatic</th><th>Why it helps</th><th>RUSM plan</th></tr></thead><tbody><tr><td>One persistent QUIC conn/node + N-stream pool (<code>NodeConnectionManager</code>) — <code>congestion/mod.rs:174</code></td><td>1 TLS handshake/node, multiplexed</td><td>Adopt</td></tr><tr><td>Deterministic stream routing <code>((src ^ dest) % streams)</code> — <code>congestion/mod.rs:244</code></td><td>in-order per process-pair, no head-of-line block</td><td>Adopt</td></tr><tr><td>⚠️ Custom 1 KiB chunking + a congestion-control worker, <em>on top of QUIC</em> — <code>congestion/mod.rs:69,99</code></td><td>QUIC already gives reliable ordered streams with per-stream + connection <strong>flow control and congestion control</strong> — re-implementing it is redundant complexity</td><td><strong>Reconsider:</strong> length-prefixed framing over QUIC streams and let QUIC apply backpressure; add app-level chunking only if profiling shows real head-of-line / fairness issues</td></tr><tr><td>Atomic message IDs + <code>DashMap</code> response cache + <code>AsyncCell</code> — <code>distributed/client.rs:85,93</code></td><td>lock-free RPC hot path; sharded reads</td><td>Adopt</td></tr><tr><td><code>rmp-serde</code> (MessagePack) wire format — <code>distributed/Cargo.toml</code></td><td>compact + fast vs JSON</td><td>Adopt (or <code>bincode</code>/<code>postcard</code>; benchmark)</td></tr><tr><td>Cert-embedded authz (X.509 ext, OID 2.5.29.9) + 100 ms keep-alive — <code>quic/quin.rs:61,145</code></td><td>auth at handshake, NAT traversal</td><td>Adopt</td></tr><tr><td>Node discovery via 5 s HTTP polling — <code>control/client.rs:287</code></td><td>simple, but laggy for fast scaling</td><td><strong>Beat:</strong> push-based discovery + pre-warmed connections</td></tr></tbody></table><h2 id="phase-10-—-performance-hardening" tabindex="-1">Phase 10 — Performance &amp; hardening <a class="header-anchor" href="#phase-10-—-performance-hardening" aria-label="Permalink to &quot;Phase 10 — Performance &amp; hardening&quot;">​</a></h2><p>Roll up the &quot;beat&quot; levers and prove them: pooling + CoW + epoch (already at ~440k component spawns/sec, ~475k wasip1 core-module spawns/sec — the direct head-to-head), quinn 0.11+ with adaptive chunking, and the superiority scorecard below — each as a measured number on the dashboard.</p><hr><h2 id="superiority-scorecard-recap" tabindex="-1">Superiority scorecard (recap) <a class="header-anchor" href="#superiority-scorecard-recap" aria-label="Permalink to &quot;Superiority scorecard (recap)&quot;">​</a></h2><p>A one-glance summary of the <strong>beat</strong> items above — all <strong>targets to validate on the dashboard</strong>, not yet achieved.</p><table tabindex="0"><thead><tr><th>Dimension</th><th>Lunatic</th><th>RUSM target</th><th>Lever (phase)</th></tr></thead><tbody><tr><td>Spawn throughput</td><td>OnDemand alloc</td><td><strong>higher</strong></td><td>pooling + CoW (6)</td></tr><tr><td>Memory / process</td><td>fresh memory per instance</td><td><strong>lower</strong></td><td>CoW-shared pages, pooled slots (6)</td></tr><tr><td>Scheduling overhead</td><td>fuel accounting</td><td><strong>lower</strong></td><td>epoch interruption (6)</td></tr><tr><td>Engine</td><td>Wasmtime 8 (2023)</td><td><strong>current</strong></td><td>modern Cranelift/async/CoW (6)</td></tr><tr><td>Connectivity</td><td>quinn 0.10, fixed chunks, 5 s poll</td><td><strong>lower latency</strong></td><td>quinn 0.11+, adaptive chunks, push discovery (9)</td></tr><tr><td>Stability under flood</td><td>unbounded mailbox</td><td><strong>bounded + observable</strong></td><td>depth limits + live observer (1)</td></tr><tr><td>Observability</td><td>metrics facade</td><td><strong>first-class</strong></td><td>HdrHistogram + observer + REPL (Phase 0, shipped)</td></tr></tbody></table><h2 id="critical-review-—-where-lunatic-looks-improvable" tabindex="-1">Critical review — where Lunatic looks improvable <a class="header-anchor" href="#critical-review-—-where-lunatic-looks-improvable" aria-label="Permalink to &quot;Critical review — where Lunatic looks improvable&quot;">​</a></h2><p>Being honest about the source we admire: beyond version-currency, several <em>design</em> choices look improvable. Each is an opportunity to <strong>evaluate with its trade-off</strong>, not a settled verdict — and several only matter at scale.</p><table tabindex="0"><thead><tr><th>Lunatic choice</th><th>The critique</th><th>Better opportunity (phase)</th></tr></thead><tbody><tr><td>Custom chunking + congestion worker over QUIC</td><td>re-implements flow/congestion control QUIC already provides — extra code + overhead</td><td>length-prefixed framing over QUIC streams; let QUIC do backpressure (9)</td></tr><tr><td>Messages + control share one <code>Signal</code> mpsc</td><td>control (<code>Kill</code>/<code>Link</code>) can sit behind a data-message flood</td><td>separate high-priority control channel (2)</td></tr><tr><td>Resource tables are <code>HashMap&lt;u64,T&gt;</code></td><td>hashing + pointer-chasing per access; ids never reused</td><td>slotmap / generational arena — array-indexed, cache-friendly, id reuse (1)</td></tr><tr><td>Two queues + a <code>Vec&lt;u8&gt;</code> per message</td><td>double handoff + per-message allocation</td><td>one queue; inline small messages + buffer pool (2)</td></tr><tr><td><code>rmp-serde</code> for intra-cluster RPC</td><td>schemaless format on a both-ends-RUSM link leaves speed on the table</td><td>zero-copy / codegen format (<code>postcard</code>, <code>rkyv</code>) for the internal wire (9)</td></tr><tr><td>stdout capture = <code>Arc&lt;RwLock&lt;Vec&lt;Mutex&lt;Cursor&gt;&gt;&gt;&gt;</code></td><td>nested locks — complexity &amp; contention smell</td><td>per-process ring buffer, or a single writer task fed by a channel (7)</td></tr><tr><td><code>Arc&lt;dyn Process&gt;</code> dynamic dispatch on the hot path</td><td>vtable indirection where the process kind is known</td><td>concrete/enum process type; reserve <code>dyn</code> for remote proxies (1–2)</td></tr></tbody></table><h3 id="dated-version-pitfalls-don-t-inherit" tabindex="-1">Dated / version pitfalls (don&#39;t inherit) <a class="header-anchor" href="#dated-version-pitfalls-don-t-inherit" aria-label="Permalink to &quot;Dated / version pitfalls (don&#39;t inherit)&quot;">​</a></h3><ul><li><strong>Wasmtime 8</strong> + <strong>quinn 0.10</strong> — well behind current; upgrading buys CoW, pooling, flow-control.</li><li><strong>OnDemand allocation</strong> + <strong>fuel</strong> preemption — leave spawn / memory / scheduling wins on the table.</li><li><strong><code>Mutex</code> on read-heavy timeout fields</strong> → <code>RwLock</code>. <strong>Fixed 1 KiB chunks</strong> + <strong>5 s HTTP polling</strong> → adaptive / push.</li><li><strong>No TLS session resumption</strong>; <strong>unbounded signal mailbox</strong> (flood risk).</li></ul><blockquote><p>Caveat (intellectual honesty): Lunatic is battle-tested and shipped; some of these are deliberate simplicity/portability trade-offs, and a few &quot;wins&quot; only show up at high scale. We validate each on the dashboard before claiming it.</p></blockquote><h1 id="maintaining-this-document" tabindex="-1">Maintaining this document <a class="header-anchor" href="#maintaining-this-document" aria-label="Permalink to &quot;Maintaining this document&quot;">​</a></h1><p>Update at the end of each phase: flip the matrix cells RUSM now implements, record the <strong>measured</strong> spawn/sec, memory/process, and latency vs the targets above (screenshot or numbers from the dashboard), and note any technique we adopted, modernized, or rejected — with the reason.</p>',53)])])}const u=t(r,[["render",n]]);export{m as __pageData,u as default};
