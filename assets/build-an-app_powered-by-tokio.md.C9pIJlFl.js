import{_ as t,o,c as s,a2 as a}from"./chunks/framework.uw_ZoRon.js";const u=JSON.parse('{"title":"Powered by Tokio","description":"","frontmatter":{},"headers":[],"relativePath":"build-an-app/powered-by-tokio.md","filePath":"build-an-app/powered-by-tokio.md"}'),n={name:"build-an-app/powered-by-tokio.md"};function r(i,e,h,d,c,p){return o(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="powered-by-tokio" tabindex="-1">Powered by Tokio <a class="header-anchor" href="#powered-by-tokio" aria-label="Permalink to &quot;Powered by Tokio&quot;">​</a></h1><p>Every RUSM process is a Tokio task. That single decision is why one machine runs <em>millions</em> of them, why blocking code becomes async for free, and why fairness is structural instead of bolted on. You just met the process; this page is the engine underneath it — and why that engine is one you can trust with production load.</p><h2 id="what-is-tokio" tabindex="-1">What is Tokio? <a class="header-anchor" href="#what-is-tokio" aria-label="Permalink to &quot;What is Tokio?&quot;">​</a></h2><p><a href="https://tokio.rs" target="_blank" rel="noreferrer">Tokio</a> is the async runtime for Rust. It schedules async tasks, drives I/O (TCP, timers, file I/O) without blocking threads, and ships a work-stealing thread pool that keeps every CPU core busy. It is <strong>not</strong> something RUSM built — it is the most widely used async runtime in the Rust ecosystem, battle-proven in production at <strong>Amazon, Discord, Cloudflare, Fly.io, and hundreds of other companies</strong> at massive scale. When you run a RUSM node, Tokio is the engine that every single process runs on.</p><h2 id="why-it-matters-for-you" tabindex="-1">Why it matters for you <a class="header-anchor" href="#why-it-matters-for-you" aria-label="Permalink to &quot;Why it matters for you&quot;">​</a></h2><p>When you spawn a RUSM process, you&#39;re creating a <strong>Tokio task</strong> — a lightweight async unit of execution. Tokio tasks cost a few hundred bytes of memory and a handful of nanoseconds to create. You can have tens of thousands of them on a single machine without breaking a sweat.</p><p>Tokio&#39;s scheduler does three things that matter deeply for RUSM:</p><p><strong>Work-stealing.</strong> Tasks are distributed across a thread pool that matches your CPU core count. An idle worker thread steals tasks from busy ones. The result: all cores stay utilized, and no single slow process can hog a thread and starve others. Fairness is structural, not something RUSM had to implement.</p><p><strong>Non-blocking I/O.</strong> When a RUSM process calls <code>receive()</code> and there&#39;s nothing in its mailbox, it doesn&#39;t block a thread — it suspends its task and yields the thread to other work. The thread is free to run thousands of other processes while this one waits. This is how RUSM achieves <strong>~21M messages/sec</strong> with <strong>p50 round-trip latency under 1µs</strong>: the overhead of a context switch is a task yield, not a kernel thread park.</p><p><strong>Cooperative preemption.</strong> RUSM adds epoch-based preemption on top of Tokio so that a CPU-bound WebAssembly guest (an infinite loop, heavy computation) doesn&#39;t monopolize a thread. The runtime periodically checks whether a Wasm instance has run too long and yields it. You get cooperative multitasking with a safety net — long-running guests don&#39;t starve their neighbors.</p><h2 id="one-process-one-tokio-task" tabindex="-1">One process = one Tokio task <a class="header-anchor" href="#one-process-one-tokio-task" aria-label="Permalink to &quot;One process = one Tokio task&quot;">​</a></h2><p>The mapping is exact and intentional:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes rose-pine-dawn rose-pine-moon catppuccin-latte catppuccin-mocha vitesse-light vitesse-dark one-light one-dark-pro vp-code" tabindex="0"><code><span class="line"><span>rusm process  ─────►  Tokio task</span></span>
<span class="line"><span>                       ├── one Wasm instance (for Wasm guests)</span></span>
<span class="line"><span>                       ├── one mailbox</span></span>
<span class="line"><span>                       └── one abort handle (for kill)</span></span></code></pre></div><p>Every process is exactly one Tokio task. No thread per process (that would cap you at thousands, not millions). No green thread runtime RUSM invented itself (that would be unproven). Just the Tokio task abstraction, which has been hardened over years of production use.</p><p><code>kill</code> cancels the Tokio task via its <strong>abort handle</strong> — one field, one call, no second signal channel. Exit signals and monitor notifications arrive in the mailbox as ordinary messages — the same queue, the same receive loop. Nothing special to wire up.</p><h2 id="the-numbers" tabindex="-1">The numbers <a class="header-anchor" href="#the-numbers" aria-label="Permalink to &quot;The numbers&quot;">​</a></h2><p>Measured on a laptop under everyday load, using RUSM&#39;s published benchmarks:</p><table tabindex="0"><thead><tr><th>Benchmark</th><th>Number</th></tr></thead><tbody><tr><td>Native process spawns/sec (Wasm-free OTP core)</td><td>~2.4M</td></tr><tr><td>Sandboxed Wasm component spawns/sec</td><td>~440k</td></tr><tr><td>Messages/sec (round-trip, two processes)</td><td>~21M</td></tr><tr><td>Round-trip latency p50</td><td>&lt;1µs</td></tr><tr><td>Concurrent connections (connection-storm bench)</td><td>thousands</td></tr></tbody></table><p>These are not theoretical ceilings — they are measured numbers from <code>rusm-bench</code> on real hardware. The limit in practice is your machine&#39;s memory and OS file descriptor count. RUSM imposes no fixed process cap.</p><h2 id="what-this-means-in-practice" tabindex="-1">What this means in practice <a class="header-anchor" href="#what-this-means-in-practice" aria-label="Permalink to &quot;What this means in practice&quot;">​</a></h2><p>You don&#39;t need to think about thread pools, connection pools, or worker queues. You spawn a process for each unit of work — one per HTTP request, one per WebSocket connection, one per background job — and Tokio handles the scheduling. If a process blocks waiting for a message, Tokio runs other processes on that thread. If a process crashes, nothing else is affected. The runtime has done this for you.</p><p>This is why RUSM can make the claim: <strong>write blocking code, the runtime makes it async</strong>. You write a straightforward <code>receive()</code> call; under the hood it&#39;s a Tokio task yield. The transformation is transparent and zero-cost.</p><hr><p>Next: <a href="/rusm/build-an-app/pid-and-self">Pid &amp; self</a> — the process identity in detail.</p>`,24)])])}const m=t(n,[["render",r]]);export{u as __pageData,m as default};
