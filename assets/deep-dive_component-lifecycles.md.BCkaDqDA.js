import{_ as t,o,c as r,a2 as a}from"./chunks/framework.uw_ZoRon.js";const m=JSON.parse('{"title":"Component lifecycles — overview","description":"","frontmatter":{},"headers":[],"relativePath":"deep-dive/component-lifecycles.md","filePath":"deep-dive/component-lifecycles.md"}'),n={name:"deep-dive/component-lifecycles.md"};function s(i,e,d,c,l,h){return o(),r("div",null,[...e[0]||(e[0]=[a('<h1 id="component-lifecycles-—-overview" tabindex="-1">Component lifecycles — overview <a class="header-anchor" href="#component-lifecycles-—-overview" aria-label="Permalink to &quot;Component lifecycles — overview&quot;">​</a></h1><p>Every component in RUSM — an HTTP handler, a WebSocket worker, an SSE stream, a per-call worker, a resident service — is the <strong>same runtime object: a process</strong>. One Wasm instance = one Tokio task, one mailbox, one fiber, one capability set. The lifecycle is therefore <em>uniform</em>; what differs by type is only <strong>when</strong> a process is spawned and <strong>how</strong> it ends.</p><p>This overview covers what&#39;s shared — the two domains, the phases, and the failure vocabulary. Then there&#39;s <strong>a chapter per component type</strong>, each walking that type&#39;s events (normal, disconnect, connection error, crash, memory/OOM crash, kill):</p><ul><li><a href="/rusm/build-an-app/serve-http"><strong>HTTP component</strong></a> — a fresh instance per request.</li><li><a href="/rusm/build-an-app/serve-sse"><strong>SSE component</strong></a> — a per-request streaming feed.</li><li><a href="/rusm/build-an-app/serve-websocket"><strong>WebSocket component</strong></a> — one process per connection.</li><li><a href="/rusm/build-an-app/run-one-off-work"><strong>Worker component</strong></a> — non-serving, spawned per call.</li><li><a href="/rusm/build-an-app/build-a-stateful-service"><strong>Service component</strong></a> — non-serving, resident and stateful.</li></ul><p>(A stock <a href="#other-wasi-cli-command-components"><code>wasi:cli</code> command</a> component is covered briefly at the end.)</p><h2 id="the-two-domains" tabindex="-1">The two domains <a class="header-anchor" href="#the-two-domains" aria-label="Permalink to &quot;The two domains&quot;">​</a></h2><p>The lens that matters most is the <strong>two domains</strong> the lifecycle runs through:</p><ul><li><strong>Platform domain</strong> — what <strong>RUSM owns</strong>: routing, spawning, the scheduler and fibers, the mailbox, socket ownership, the reply/stream plumbing, capability and memory enforcement, exit detection, links/monitors, and the supervisor. You never write this.</li><li><strong>Application domain</strong> — what <strong>you write</strong>: the handler action, the service functions, the worker body. Pure logic — no router, no wire, no socket, no <code>async</code>, no lifecycle bookkeeping.</li></ul><p>The boundary is deliberate and load-bearing: a failure in the application domain is <em>caught by the platform domain</em> and turned into a status code, a <code>Down</code> message, or a supervisor restart — it is never allowed to take down the listener, a sibling, or the node. Every per-type chapter is organised around this split: <strong>&quot;Platform owns / You write,&quot;</strong> then an events table whose columns are <em>Platform domain</em> and <em>Application domain</em>.</p><h2 id="who-owns-what-phase-by-phase" tabindex="-1">Who owns what, phase by phase <a class="header-anchor" href="#who-owns-what-phase-by-phase" aria-label="Permalink to &quot;Who owns what, phase by phase&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Phase</th><th>Platform domain (RUSM)</th><th>Application domain (you)</th></tr></thead><tbody><tr><td><strong>Register</strong></td><td>resolve a name → a prepared component (compiled module + linked imports)</td><td>declare <code>[[serve]]</code> / <code>[components.&lt;name&gt;]</code> / <code>[serve.routes]</code></td></tr><tr><td><strong>Spawn</strong></td><td>instantiate on the pooling allocator + copy-on-write + <code>InstancePre</code> (~440k/s); apply default-deny <strong>capabilities</strong> + a <code>StoreLimiter</code> memory ceiling; create the mailbox, fiber, and abort handle</td><td>—</td></tr><tr><td><strong>Dispatch</strong></td><td>match the route, own the socket, spawn the handler, send the request over the actor wire, run the ephemeral reply <em>responder</em></td><td>—</td></tr><tr><td><strong>Run</strong></td><td>suspend/resume the fiber on every blocking call (<code>receive</code>, <code>Stream::read</code>, a service <code>call</code>); enforce <a href="/rusm/deep-dive/epoch-preemption">epoch preemption</a> on CPU-bound guests</td><td>your action / service / worker body — straight-line, blocking-looking code</td></tr><tr><td><strong>Reply / stream</strong></td><td>turn your <code>Response</code> into the HTTP response; drain your byte stream into the chunked body with back-pressure; own the WS socket sink</td><td><code>return</code> a <code>Response</code>, or <code>sse.data(…)</code>, or <code>conn.send(…)</code></td></tr><tr><td><strong>Exit</strong></td><td>classify the exit (Normal / Crashed / Killed), reclaim the instance + mailbox + streams, fire links and monitors, drive the supervisor</td><td>optionally <code>monitor</code> / <code>Supervisor</code></td></tr></tbody></table><p><strong>Blocking is async, for free.</strong> In the Run phase your code can call <code>receive</code> or read a stream and <em>block</em> — the platform suspends the <a href="/rusm/deep-dive/fibers-and-blocking-to-async">fiber</a> and frees the scheduler thread until data arrives, then resumes you. You write sequential code; the platform makes it cooperative.</p><h2 id="the-shared-failure-vocabulary" tabindex="-1">The shared failure vocabulary <a class="header-anchor" href="#the-shared-failure-vocabulary" aria-label="Permalink to &quot;The shared failure vocabulary&quot;">​</a></h2><p>Every component ends in one of these ways. The per-type chapters refer back to these definitions rather than repeat them.</p><table tabindex="0"><thead><tr><th>Outcome</th><th>What it is</th><th>Domain that originates it</th></tr></thead><tbody><tr><td><strong>Normal exit</strong></td><td>your entrypoint returned</td><td>application (clean finish)</td></tr><tr><td><strong>Crash (trap)</strong></td><td>a guest fault — <code>panic!</code>, <code>.unwrap()</code> on <code>None</code>/<code>Err</code>, <code>unreachable</code>, an out-of-bounds access, or a capability violation that traps. Wasmtime raises a trap; the platform marks the process <strong>Crashed</strong> with the trap as the exit reason</td><td>application fault, <strong>caught</strong> by the platform</td></tr><tr><td><strong>Memory crash (OOM)</strong></td><td>the guest tries to grow linear memory past its <code>StoreLimiter</code> ceiling (the capability profile&#39;s <code>max-memory-mb</code>). The allocation fails → trap → <strong>Crashed</strong>. It is an ordinary crash — bounded to that one instance, never the node</td><td>application fault, <strong>bounded</strong> by the platform</td></tr><tr><td><strong>Kill</strong></td><td>an external <code>kill</code>, or an exit cascade from a crashed <strong>link</strong>. The platform fires the process&#39;s <code>futures</code> abort handle; the signal rides the same mailbox (no second channel)</td><td>platform / supervision</td></tr><tr><td><strong>Client disconnect</strong></td><td>the peer closed the socket. <strong>Not a crash</strong> — the platform surfaces it (a failed write or a closed request body) and the unit ends cleanly</td><td>platform (detected), application (observes via a <code>false</code> write)</td></tr><tr><td><strong>Connection error</strong></td><td>a socket- or protocol-level failure <em>around</em> your code: a malformed request line, an unreadable body, a write to a half-open socket. Usually your code never runs — the platform answers directly</td><td>platform</td></tr></tbody></table><p>On any exit the platform <strong>reclaims</strong> the instance (returned to the pool), the mailbox, and any byte streams, then notifies <strong>links</strong> (an exit cascade, unless the peer set <code>trap_exit</code>) and <strong>monitors</strong> (a <code>Down</code> carrying the reason). A <strong>supervisor</strong> restarts per its strategy (one-for-one / one-for-all / rest-for-one) under <a href="/rusm/deep-dive/links-and-supervision">windowed restart-intensity</a> — too many restarts in the window and it escalates instead of crash-looping.</p><h2 id="why-a-failure-stays-local" tabindex="-1">Why a failure stays local <a class="header-anchor" href="#why-a-failure-stays-local" aria-label="Permalink to &quot;Why a failure stays local&quot;">​</a></h2><p>The two-domain boundary is what makes the blast radius of any failure exactly <strong>one unit of work</strong> — the guarantee every chapter relies on:</p><ul><li><strong>Isolation by instance (platform).</strong> Each request/connection/worker is its own Wasm instance with its own linear memory and capabilities. A trap or OOM can corrupt only that instance&#39;s memory, which is discarded on exit.</li><li><strong>No head-of-line blocking (platform).</strong> Serving is process-per-unit, so a slow, stuck, or crashed handler never holds up the listener or other clients.</li><li><strong>Crash containment (platform).</strong> An application-domain fault becomes a <code>Crashed</code> exit — surfaced as a <strong>502</strong>, a dropped connection, or a <code>Down</code>/exit signal — never a node-wide failure. Per-request work just gets a fresh instance; resident work is restarted (or escalated) by a supervisor.</li><li><strong>No leaks, no spins (platform).</strong> Exit reclaims the instance, mailbox, and streams; byte streams are bounded and back-pressured, so a producer parks rather than spins, and a disconnect always tears the process down.</li></ul><p>You write the application domain and reason about one request at a time; the platform domain guarantees the rest.</p><h2 id="other-wasi-cli-command-components" tabindex="-1">Other: <code>wasi:cli</code> command components <a class="header-anchor" href="#other-wasi-cli-command-components" aria-label="Permalink to &quot;Other: `wasi:cli` command components&quot;">​</a></h2><p>A stock command component (<code>WasmRuntime::spawn_command</code>) has no actor world — just <code>wasi:cli/run</code>. <strong>Normal:</strong> runs <code>run</code>, exits with code <strong>0</strong>. <strong>Crash / non-zero exit / OOM:</strong> <strong>Crashed</strong>, the exit reason carrying the status. Same isolation and reclaim as any process; it just has no serving or messaging role.</p><p>See also: <a href="/rusm/deep-dive/the-process-model">the process model</a>, <a href="/rusm/deep-dive/links-and-supervision">links &amp; supervision</a>, <a href="/rusm/deep-dive/fibers-and-blocking-to-async">fibers &amp; blocking→async</a>, <a href="/rusm/deep-dive/permissions-and-sandboxing">permissions &amp; sandboxing</a>, and <a href="/rusm/deep-dive/the-serving-model">the serving model</a>.</p>',23)])])}const u=t(n,[["render",s]]);export{m as __pageData,u as default};
