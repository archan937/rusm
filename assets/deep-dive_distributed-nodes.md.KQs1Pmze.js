import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.uw_ZoRon.js";const g=JSON.parse('{"title":"Distributed nodes","description":"","frontmatter":{},"headers":[],"relativePath":"deep-dive/distributed-nodes.md","filePath":"deep-dive/distributed-nodes.md"}'),n={name:"deep-dive/distributed-nodes.md"};function r(i,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="distributed-nodes" tabindex="-1">Distributed nodes <a class="header-anchor" href="#distributed-nodes" aria-label="Permalink to &quot;Distributed nodes&quot;">​</a></h1><p>Distributed Erlang&#39;s superpower is that scaling to many machines barely changes your code: a process sends to a peer the same way whether that peer is in the same node or across the network. RUSM brings that to WebAssembly — and makes the wire <strong>secure by default</strong> (QUIC + mutual TLS), which the BEAM never did.</p><p>A single RUSM node is a host process running many lightweight processes. A <strong>cluster</strong> is several nodes — typically on different machines — connected so those processes can spawn and message across the boundary as if they were local: a process doesn&#39;t know (or care) which node a peer lives on. It ships in the Wasm-free <a href="https://github.com/archan937/rusm" target="_blank" rel="noreferrer"><code>rusm-cluster</code></a> crate, layered over <code>rusm-otp</code> — so distribution never drags WebAssembly into the core.</p><h2 id="connecting" tabindex="-1">Connecting <a class="header-anchor" href="#connecting" aria-label="Permalink to &quot;Connecting&quot;">​</a></h2><p>Nodes connect over <strong>QUIC</strong> with <strong>TLS 1.3</strong> (think <code>Node.connect/1</code>, but secure by default). Each node has a name; on connect, both ends exchange names over a dedicated control stream and remember the peer. Links are <strong>mutually authenticated</strong> — both ends present a certificate and verify the other against a shared trust anchor, so a peer without a trusted certificate is rejected at the handshake. Use a <code>ClusterCa</code> to issue each node its own CA-signed certificate (per-node keys, independently revocable), or a single shared <code>Identity::generate()</code> for a small/trusted cluster.</p><h2 id="location-transparency" tabindex="-1">Location transparency <a class="header-anchor" href="#location-transparency" aria-label="Permalink to &quot;Location transparency&quot;">​</a></h2><p>Once connected:</p><ul><li><strong>Cross-node <code>send</code></strong> — <code>node.send(&quot;london&quot;, &quot;greeter&quot;, bytes)</code> routes to the process registered as <code>greeter</code> on <code>london</code>; the sender doesn&#39;t open the socket itself.</li><li><strong>Global registry</strong> — <code>register_global(name, pid)</code> publishes a name cluster-wide by gossiping it to every peer; <code>send_global(name, bytes)</code> resolves the owning node and routes there, so a service is reachable by name from anywhere.</li><li><strong>Remote spawn</strong> — a node registers spawnable factories by name; a peer calls <code>spawn_remote(node, factory, args)</code> and gets back the pid spawned on that node. (A closure can&#39;t cross the wire, so a node only spawns work it&#39;s been taught — explicit and capability-friendly.)</li></ul><h2 id="wire-shape" tabindex="-1">Wire shape <a class="header-anchor" href="#wire-shape" aria-label="Permalink to &quot;Wire shape&quot;">​</a></h2><p>Each link multiplexes a single long-lived <strong>control stream</strong> (name exchange, registry gossip, the request/reply RPC behind remote spawn and live attach) and one <strong>uni-stream per message</strong> (so messages never head-of-line-block each other). On loopback the transport does ~550k cross-node messages/sec at ~39µs p50 round-trip — see the <a href="https://github.com/archan937/rusm" target="_blank" rel="noreferrer"><code>cluster_fanout</code></a> benchmark.</p><h2 id="testing-it" tabindex="-1">Testing it <a class="header-anchor" href="#testing-it" aria-label="Permalink to &quot;Testing it&quot;">​</a></h2><p>Tests boot several nodes in one process and connect them on loopback, so cross-node send, the global registry, remote spawn, and live-attach listing are all TDD-able with no external network.</p><h2 id="hooking-in" tabindex="-1">Hooking in <a class="header-anchor" href="#hooking-in" aria-label="Permalink to &quot;Hooking in&quot;">​</a></h2><p>You can also attach to any running node to inspect it live — see <a href="/rusm/deep-dive/live-attach">live attach</a>.</p>',14)])])}const u=t(n,[["render",r]]);export{g as __pageData,u as default};
