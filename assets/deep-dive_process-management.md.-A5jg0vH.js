import{_ as o,o as s,c as a,a2 as t}from"./chunks/framework.uw_ZoRon.js";const g=JSON.parse('{"title":"Process management","description":"","frontmatter":{},"headers":[],"relativePath":"deep-dive/process-management.md","filePath":"deep-dive/process-management.md"}'),r={name:"deep-dive/process-management.md"};function i(n,e,d,c,l,p){return s(),a("div",null,[...e[0]||(e[0]=[t('<h1 id="process-management" tabindex="-1">Process management <a class="header-anchor" href="#process-management" aria-label="Permalink to &quot;Process management&quot;">​</a></h1><p>Spawn, send, and receive are the basics. Beyond them, every RUSM process gets the full Erlang <strong>management toolkit</strong> — a named registry, process groups, timers, introspection, and orderly shutdown. All of it is backed by the Wasm-free <code>rusm-otp</code> core, so it works identically whether a process body is native Rust or a sandboxed Wasm instance.</p><h2 id="named-registry" tabindex="-1">Named registry <a class="header-anchor" href="#named-registry" aria-label="Permalink to &quot;Named registry&quot;">​</a></h2><p>Register a process under a name so others can reach it without holding its pid: <code>register(name)</code> / <code>whereis(name)</code>. The registry is a sharded <code>DashMap</code> (no global lock), so lookups scale with cores. For names that resolve <em>across machines</em>, see <a href="/rusm/deep-dive/distributed-nodes">distributed nodes</a>.</p><h2 id="process-groups-tags" tabindex="-1">Process groups (tags) <a class="header-anchor" href="#process-groups-tags" aria-label="Permalink to &quot;Process groups (tags)&quot;">​</a></h2><p>Where the registry maps <strong>one name to one pid</strong>, a process group (Erlang&#39;s <code>pg</code>) maps <strong>one tag to many pids</strong> — and a process may hold many tags. A process joins a group by tagging <em>itself</em> with <code>register_tag(tag)</code> (unprivileged, like <code>set_label</code>); <code>whereis_tag(tag)</code> lists the live members; <code>kill_tag(tag)</code> terminates the whole group and returns how many it killed; <code>unregister_tag(tag)</code> leaves. Memberships are released on exit by the <strong>same reaper that releases names</strong>, so a group only ever reports live processes — and tags add <strong>zero hot-path cost</strong> (an untagged process carries an empty list; the reaper loops it only at exit).</p><p><code>kill_tag</code> is the one privileged op — it terminates <em>other</em> processes — so it&#39;s gated by the <code>process-control</code> capability exactly like <code>kill</code> (a sandboxed guest gets <code>0</code>). That makes process groups the clean primitive for <strong>scoped cancellation</strong>: tag every process that belongs to one unit of work — say <code>plan:&lt;id&gt;</code> for the agents a request spawned — and a single <code>kill_tag(&quot;plan:&lt;id&gt;&quot;)</code> stops the whole unit immediately and authoritatively. The application writes only two calls — self-tag on start, gated <code>kill_tag</code> to cancel — while the platform owns the group registry, the reaping, and the gate. No cancel topics, no polling, no pid bookkeeping in app code.</p><p>Same surface from all three guests: <code>register_tag</code>/<code>kill_tag</code>/<code>whereis_tag</code> in <code>rusm-rs</code>, <code>Process.registerTag</code>/<code>killTag</code>/<code>whereisTag</code> in <code>rusm-ts</code>, <code>RegisterTag</code>/<code>KillTag</code>/<code>WhereisTag</code> in <code>rusm-go</code> — backed, like everything here, by the Wasm-free <code>rusm-otp</code> core. This group form is also RUSM&#39;s pub/sub primitive; see <a href="/rusm/build-an-app/broadcast-to-many">broadcast to many</a>.</p><h2 id="timers" tabindex="-1">Timers <a class="header-anchor" href="#timers" aria-label="Permalink to &quot;Timers&quot;">​</a></h2><p><code>send_after(to, delay_ms, msg)</code> schedules a message to arrive in <code>to</code>&#39;s mailbox after <code>delay_ms</code> milliseconds; <code>cancel_timer(handle)</code> aborts a pending one. Built on Tokio&#39;s timer wheel, so millions of outstanding timers cost almost nothing.</p><h2 id="introspection" tabindex="-1">Introspection <a class="header-anchor" href="#introspection" aria-label="Permalink to &quot;Introspection&quot;">​</a></h2><p><code>list()</code> enumerates live pids; <code>info(pid)</code> returns a process&#39;s links, monitors, names, label, mailbox depth, and whether it traps exits; <code>set_label(..)</code> tags a process for the observer. This is exactly what the <a href="/rusm/deep-dive/live-attach">live-attach</a> REPL and the dashboard observer read — nothing is hidden from you.</p><h2 id="lifecycle-kill-and-shutdown" tabindex="-1">Lifecycle: kill and shutdown <a class="header-anchor" href="#lifecycle-kill-and-shutdown" aria-label="Permalink to &quot;Lifecycle: kill and shutdown&quot;">​</a></h2><p><code>kill(pid)</code> stops one process immediately via a <code>futures</code> <strong>abort handle</strong> — no second signal channel: kill rides that handle while exit and down signals ride the mailbox. The process still runs its normal teardown, so linked and monitoring peers are notified and its names are released.</p><p><code>shutdown()</code> is the runtime-wide kill switch: it signals <strong>every</strong> live process to stop (each running that same orderly teardown) and returns how many it signalled. Teardown is asynchronous, so poll <code>process_count()</code> to wait for the drain. Crashes and exits flow through <a href="/rusm/deep-dive/links-and-supervision">links &amp; supervision</a>.</p><blockquote><p>Registry, timers, and graceful shutdown shipped in Phase 4; introspection, labels, and mailbox depth surface in the observer snapshot.</p></blockquote>',16)])])}const m=o(r,[["render",i]]);export{g as __pageData,m as default};
