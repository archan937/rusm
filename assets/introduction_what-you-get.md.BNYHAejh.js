import{_ as o,o as s,c as r,a2 as t}from"./chunks/framework.uw_ZoRon.js";const m=JSON.parse('{"title":"What you get","description":"","frontmatter":{},"headers":[],"relativePath":"introduction/what-you-get.md","filePath":"introduction/what-you-get.md"}'),a={name:"introduction/what-you-get.md"};function n(i,e,c,d,l,u){return s(),r("div",null,[...e[0]||(e[0]=[t('<h1 id="what-you-get" tabindex="-1">What you get <a class="header-anchor" href="#what-you-get" aria-label="Permalink to &quot;What you get&quot;">​</a></h1><p><strong>RUSM is Erlang/OTP&#39;s concurrency and fault-tolerance model, rebuilt in Rust, where every process can be a sandboxed WebAssembly component</strong> — &quot;let it crash&quot; meets &quot;run untrusted code safely,&quot; at millions of lightweight processes, with none of the wasmCloud ceremony. The whole design rests on one principle: the actor core (<code>rusm-otp</code>) is <strong>Wasm-free</strong>; WebAssembly is a pluggable process backend. That&#39;s why it&#39;s both a great native actor runtime <em>and</em> a great Wasm host.</p><p>This page is the map. Each item links to the <a href="/rusm/deep-dive/the-process-model">concept</a> that teaches it — skim for what you need, then follow the arrow.</p><h2 id="concurrency-the-actor-core" tabindex="-1">Concurrency &amp; the actor core <a class="header-anchor" href="#concurrency-the-actor-core" aria-label="Permalink to &quot;Concurrency &amp; the actor core&quot;">​</a></h2><p>The Erlang core, rebuilt in Rust and Tokio — processes, mailboxes, supervision, fairness:</p><ul><li><strong>Massive lightweight concurrency (Tokio-driven)</strong> — hundreds of thousands of processes over a few OS threads; spawning is near-free (~2.4M/sec measured). → <a href="/rusm/deep-dive/the-process-model">the process model</a></li><li><strong>Isolated, lightweight processes</strong> — one process = one task (and, for Wasm, one isolated instance); no shared mutable state.</li><li><strong>Superior messaging</strong> — per-process mailboxes, by-value messages, selective receive, ~21M msgs/sec. → <a href="/rusm/deep-dive/message-passing">message passing</a></li><li><strong>Fault tolerance &amp; supervision</strong> — links, monitors, <code>trap_exit</code>, <code>spawn_link</code>, <code>exit</code>, one-for-one / one-for-all / rest-for-one, windowed restart-intensity. → <a href="/rusm/deep-dive/links-and-supervision">links &amp; supervision</a></li><li><strong>Preemptive fairness</strong> — a runaway guest can&#39;t starve others; Wasmtime epoch interruption forces it to yield. → <a href="/rusm/deep-dive/epoch-preemption">epoch preemption</a></li><li><strong>&quot;Write blocking code, get async&quot;</strong> — <code>receive</code> parks the fiber and frees the worker. → <a href="/rusm/deep-dive/fibers-and-blocking-to-async">fibers &amp; blocking→async</a></li><li><strong>Process management</strong> — named registry, timers, graceful shutdown, full introspection. → <a href="/rusm/deep-dive/process-management">process management</a></li><li><strong>Backpressure &amp; overload</strong> — bounded byte-stream channels; opt-in bounded mailboxes that shed <em>user</em> messages but never system/exit signals.</li></ul><h2 id="webassembly-safety" tabindex="-1">WebAssembly &amp; safety <a class="header-anchor" href="#webassembly-safety" aria-label="Permalink to &quot;WebAssembly &amp; safety&quot;">​</a></h2><p>What makes a process a <em>sandboxed</em> process — the component model, default-deny capabilities, and trap isolation:</p><ul><li><strong>WASM components (WASI p2 + p3)</strong> — the modern component model, not just core modules (the headline difference from Lunatic). → <a href="/rusm/deep-dive/components-and-the-actor-world">components &amp; the actor world</a></li><li><strong>WASM core modules (wasip1)</strong> — also runs raw preview-1 modules via a bridge.</li><li><strong>Default-deny capability sandboxing</strong> — <code>sandboxed</code> / <code>network-client</code> / <code>trusted</code> profiles + per-spawn overrides; per-instance memory caps; spawn-from-guest never escalates. → <a href="/rusm/deep-dive/permissions-and-sandboxing">permissions &amp; sandboxing</a></li><li><strong>Trap isolation</strong> — a guest trap becomes that one process <code>Crashed</code>; the runtime and its neighbours are untouched.</li><li><strong>Durable key-value storage</strong> — an embedded, transactional store (<code>rusm-kv</code>/redb, no daemon) behind the default-deny <code>allow-storage</code> capability; the <code>kv</code> API is the same in Rust and TS. → <a href="/rusm/deep-dive/permissions-and-sandboxing">permissions &amp; sandboxing</a></li><li><strong>Guests in Rust, TypeScript, <em>or</em> Go</strong> — the <code>#[service]</code> macro, the concealed typed client, the <strong>shared rquickjs runner</strong> (tiny TS components vs jco baking an engine into every one) + bytecode precompile, plus native <code>fetch</code> and <strong><code>crypto.subtle</code></strong> (SHA/HMAC/AES-GCM) for TS; Go compiles via TinyGo to <code>wasm32-wasip2</code>, same actor world and wire. → <a href="/rusm/deep-dive/guests">guests: Rust, TypeScript &amp; Go</a></li></ul><h2 id="serving-streaming" tabindex="-1">Serving &amp; streaming <a class="header-anchor" href="#serving-streaming" aria-label="Permalink to &quot;Serving &amp; streaming&quot;">​</a></h2><p>Run a component as a real web server — one process per request or per connection, never a shared instance:</p><ul><li><strong>HTTP / WebSocket / SSE serving</strong> — from a Rust, TypeScript, or Go component, always <strong>process-per-unit-of-work</strong>: a fresh sandboxed instance per HTTP/SSE request, one sandboxed process per WS connection. No head-of-line blocking, crash containment, and full isolation by construction; cheap on the pooled spawn path (~440k spawns/sec). → <a href="/rusm/deep-dive/the-serving-model">the serving model</a></li><li><strong>Declarative routing</strong> — a per-listener <code>rusm.toml</code> <code>[serve.routes]</code> subtable (one per <code>[[serve]]</code> HTTP/SSE listener, so multiple ports route independently) maps <code>&quot;METHOD /path/:param&quot; = &quot;component#action&quot;</code> (<code>:name</code> path param, trailing <code>*</code> wildcard); the host gateway resolves it (most-specific wins; 405 on method mismatch, 404 on no match). Rust handlers are <code>pub fn</code>s under <code>#[rusm_rs::handlers]</code> — no <code>main</code>, no router code.</li><li><strong>Streaming &amp; async</strong> — SSE and WS as per-connection handler processes (<code>open</code>/<code>message</code>/<code>close</code>, one process per connection, streaming for its life), Tokio back-pressure throughout.</li><li><strong>Shared state lives elsewhere</strong> — never in the ephemeral serving instance: a long-lived <code>[components.&lt;name&gt;]</code> service (<code>resident = true</code>) reached over the actor API (<code>whereis</code> / <code>call</code> / <code>send</code>), or durable <code>kv</code>. Pairs with the <code>pubsub::Topics</code> primitive (keyed fan-out, monitor-based pruning): one publish → every connected client, no subscriber bookkeeping in app code.</li><li><strong>Cross-process byte streams</strong> — a bounded, back-pressured byte channel between processes. → <a href="/rusm/deep-dive/byte-streams">byte streams</a></li></ul><h2 id="apps-clusters-dx" tabindex="-1">Apps, clusters &amp; DX <a class="header-anchor" href="#apps-clusters-dx" aria-label="Permalink to &quot;Apps, clusters &amp; DX&quot;">​</a></h2><p>One CLI from scaffold to live node, plus cross-node clustering and live attach:</p><ul><li><strong>App model</strong> — <code>rusm.toml</code> describes components and servers; <code>rusm build</code> (cargo wasm32-wasip2 / Bun, no jco) → <code>./wasm/</code>. → <a href="/rusm/deep-dive/the-app-model">the app model</a></li><li><strong>CLI</strong> — <code>rusm new</code> (scaffold), <code>rusm run</code>, <code>rusm serve</code>, <code>rusm dev</code> (watch + reload), <code>rusm attach</code> (a live REPL into a local or remote node).</li><li><strong>Custom bridges</strong> — give your guests a native host function the platform doesn&#39;t provide (a DB client, an internal API, a signing routine): define it once in Rust and call it from <strong>any</strong> guest — TypeScript, Rust, <strong>and</strong> Go — as an ordinary typed import. RUSM&#39;s compiled-in answer to a capability provider: no lattice, no broker, default-deny and gated by name. → <a href="/rusm/build-an-app/add-your-own-functions">add your own functions</a></li><li><strong>Dynamic JS</strong> — load a guest&#39;s JS from a URL or the durable <code>kv</code> store (<code>source = &quot;…&quot;</code>) to <strong>deploy live with no node rebuild</strong>, or run code chosen <em>at runtime</em> — generated, fetched, or user-submitted — inside an operator-defined sandbox (<code>dynamic = &quot;js&quot;</code> + <code>spawn-from</code>): the guest picks the code, never the capabilities. Drivable from TypeScript, Rust, and Go. → <a href="/rusm/build-an-app/dynamic-js">Dynamic JS</a></li><li><strong>Dynamic WASM</strong> — the compiled twin of Dynamic JS: spawn a <strong>compiled WASM component chosen at runtime</strong> (<code>dynamic = &quot;wasm&quot;</code> + <code>spawn-from</code>, or a remote <code>source</code>), compiled once then served <strong>hot</strong> from a content-addressed cache — ~17 ms cold → ~0.5 ms hot. The plugin can be any wasip2 component (Rust, Go, …); driven from any guest. → <a href="/rusm/build-an-app/dynamic-wasm">Dynamic WASM</a></li><li><strong>Distributed clustering</strong> — <code>ClusterNode::connect</code> (the <code>Node.connect</code> equivalent), cross-node send, a gossiped global registry, remote spawn, all over QUIC + <strong>mutual TLS</strong>. → <a href="/rusm/deep-dive/distributed-nodes">distributed nodes</a></li><li><strong>Live attach + a JS REPL</strong> — inspect <em>and drive</em> a running node: a stateful JavaScript shell into the live process table (<code>whereis</code>/<code>send</code>/<code>kill</code>/<code>connect</code>), like Erlang&#39;s <code>iex --remsh</code>. → <a href="/rusm/deep-dive/the-live-repl">the live REPL</a>, <a href="/rusm/deep-dive/live-attach">live attach</a></li><li><strong>DX: infra never bothers you</strong> — you write <em>application functions</em>; RUSM owns <em>all</em> the infrastructure (spawn/receive/reply/supervise/sockets).</li><li><strong>No funky rules</strong> — no execution-time cap, no &quot;this must be a service,&quot; no lattice/provider ceremony.</li></ul><h2 id="observability-quality" tabindex="-1">Observability &amp; quality <a class="header-anchor" href="#observability-quality" aria-label="Permalink to &quot;Observability &amp; quality&quot;">​</a></h2><p>You can see what the runtime is doing, and the numbers are honest:</p><ul><li><strong>Live process statistics</strong> — an Erlang-<code>observer</code>-style view (process count, scheduler load, memory, per-instance table), nearly free.</li><li><strong>Live benchmark dashboard</strong> — real scenarios streamed to a React/uPlot UI. → <a href="/rusm/about/benchmark-dashboard-and-observer">benchmark &amp; dashboard</a></li><li><strong>Fair, out-of-process benchmarking</strong> — <code>rusm-loadtest</code> drives a real <code>rusm serve</code> port from a separate process, so the numbers are the server&#39;s.</li><li><strong>TDD, ~100% coverage; OTP first, WASM second</strong> — the dependency graph enforces the Wasm-free core. → <a href="/rusm/about/architecture">architecture</a></li></ul><h2 id="where-it-s-going" tabindex="-1">Where it&#39;s going <a class="header-anchor" href="#where-it-s-going" aria-label="Permalink to &quot;Where it&#39;s going&quot;">​</a></h2><p>The core is audited-solid today; 0.3.0 matured the serving surface (connection context, WebSocket framing + permessage-deflate, rich SSE events, per-listener resource/CSWSH controls, compression, and native <strong>TLS</strong>). <strong>Phase 12</strong> (the serve-path admission-control remainder — request-body cap + per-request timeout → <code>503</code> — default-bounded serve mailboxes, and cluster gossip authentication) is explicitly planned. See the <a href="/rusm/about/roadmap">roadmap</a>.</p>',20)])])}const h=o(a,[["render",n]]);export{m as __pageData,h as default};
