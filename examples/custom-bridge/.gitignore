# Build output. The host glue (./wit, src/bindings.rs, src/bridges.rs) IS committed so the
# host crate compiles in the workspace; a guest component's wit/ is regenerated by `rusm
# build` from bridges/ on every build, so it's output, not source.
/wasm/
/components/*/wit/
