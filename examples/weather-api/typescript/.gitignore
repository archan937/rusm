# Build output. `bridges.d.ts` IS committed so the TS guest compiles with typed ambient
# declarations; the Cargo/src/wit host glue is generated by `rusm build` and kept git-ignored
# since this flavour's host binary is generated (not authored).
/wasm/
/target/
/Cargo.toml
/Cargo.lock
/src/
/wit/
/bridges/weather/_runner.ts
/node_modules/
/bun.lock
/components/*/wit/
/components/*/internal/
